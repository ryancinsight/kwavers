//! Reserved namespace for the geometry vertical slice.
//!
//! This module is empty. Board geometry — the `Nm` and `Mm` length newtypes, `Point`,
//! `GridSpec`, and the distance and convex-hull helpers — lives in [`crate::geom`], which
//! is the authoritative surface. The directory reserves the slice name for the split
//! recorded in `docs/MIGRATION.md`; until that lands, import from [`crate::geom`].
