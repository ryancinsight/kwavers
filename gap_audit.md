# Gap Audit

## Initial Findings (Sprint Start)
- Current structure is now tracked with architecture guards and cross-contamination is closed at the verified boundary level required by the canonical plan.
- Required to validate `pykwavers` against `k-wave-python`. Needs formal suite for 1-to-1 parity mapping.
- Deep nested file structures (3-5+ levels) required but may not be fully conforming.
- `pykwavers` needs to solely represent thin `PyO3` wrappings over `kwavers`, any core fixes must be bubbled down to `kwavers` itself.

## Priority Matrix
1. [Highest] Locate and prune circular dependencies and duplicate/inconsistent implementations across core modules.
2. [High] Define the validation suite matrix for Grid, Source, Signal, Sensor, Solver.
3. [Medium] Ensure GPU support is routed correctly via BURN crate.

## Outstanding Gaps (Newly Identified 2026-04-22)
- `at_linear_array_transducer` parity is closed: the parity example now uses the upstream additive pressure-source mode, source rows remain Fortran-ordered, and the rebuilt extension passes the `p_max` field comparison.
- `at_focused_annular_array_3D` parity closed in the per-element annular path (`KWaveArray::add_annular_element` / `add_annular_array` and `Source.from_kwave_array_per_element`).
- DICOM SSOT violation (CLOSED 2026-05-01): all three SSOT violations are resolved. `infrastructure::io::dicom_ritk` is now the single adapter wrapping `ritk_io::scan_dicom_directory` + `ritk_io::load_dicom_series::<NdArray>` and converting ritk-io's `Image<B, 3>` → kwavers `Array3<f64>` + `MedicalImageMetadata`; `DicomImageLoader::load_series_internal` delegates to it; the parallel `infrastructure/io/dicom.rs` (684-line `dicom`-crate-direct reader, zero callers) and orphaned `src/bin_test.rs` smoke stub are deleted; the direct `dicom = "0.7"` dep in `kwavers/Cargo.toml` is dropped (now pulled transitively through ritk-io). Plus the earlier 2026-04-30 work that made ritk-core/ritk-io/burn mandatory and reduced the `ritk`/`pinn`/`dicom` features to no-op aliases. Full lib suite passes 2640/2640 with 12 ignored.

## Resolved Since Audit Start
- Closed the Chapter 29 linear/nonlinear comparison-control gap: the book
  generator now runs a matched linear case at the nonlinear grid, element
  count, drive frequency, and source pressure, projects both outputs onto the
  nonlinear crop grid, records compressed intermediate fields plus metrics, and
  documents the measured nonlinear pressure outside-target energy and residual
  projected aperture mismatch. The follow-on correction changes the nonlinear
  histotripsy path from diagnostic-like response to MI-gated inertial
  cavitation, preserves per-element source drive before calibration, reports
  actual aperture counts, expands the brain cap aperture to the requested
  grid-supported element count, and constrains passive cavitation inversion to
  the MI-gated Rayleigh-Plesset source support. The controlled Figure 6 artifact
  now renders every comparison field on the same full-resolution CT/transducer
  placement grid as Figure 2 and Figure 5. Regenerated controlled metrics show
  nonlinear fusion improves over the reduced linear path, while passive
  cavitation still fails lesion Dice because the MI-gated source support remains
  off target before passive inversion.
- Closed the CT-aligned brain pose/target drift gap: `CANONICAL_BRAIN_SCENE`
  now stores the VIM-like target fraction and Insightec-like transducer pose
  once, and the Chapter 25 figure workflow, Chapter 29 Figure 5 brain
  simulation, 3-D helmet placement wrappers, Chapter 31 brain geometry, and the
  skull-adaptive transcranial benchmark resolve their target and geometry from
  that shared scene instead of using independent centroids, benchmark element
  counts, or duplicated cap defaults.
- Closed the solver/domain/simulation cross-contamination gap by removing the obsolete `domain::factory` solver assembly artifact, moving concrete FDTD/PSTD/Hybrid construction to `simulation::solver_factory`, reducing `solver::factory` to descriptor-based selection and memory-budget policy, and adding architecture tests that compute forbidden import edges.
- Closed the simulation progress-reporting boundary leak by making `solver::interface::progress` the authoritative contract, reducing `solver::progress` to a compatibility re-export, updating `simulation::core` to consume the interface path, and adding `architecture_boundaries::simulation_progress_reporting_uses_solver_interface_boundary`.
- Closed the FDTD configuration drift gap by routing full test/example literals through `FdtdConfig::default()` for defaulted fields, preserving the Cartesian geometry default after the required `geometry` field was added.
- Closed the seismic FWI example API drift by adding `FwiProcessor::generate_synthetic_data` as the public synthetic-data boundary over the canonical forward model and replacing obsolete 3-D observed-data calls with the current receiver-time `Array2` contract.
- Closed the PSTD absorption diagnostic gap by making `initialize_absorption_operators` crate-visible to match `AbsorptionKernel` and removing the unreachable `Ok(None)` tail from the exhaustive absorption-mode match.
- Tone-burst sample count now follows the k-wave-python floor-plus-one endpoint rule.
- `pykwavers.tone_burst()` now defaults to the k-wave Gaussian envelope and matches the vendored reference waveform after flattening the row vector.
- Seismic FWI now uses the physical L2 residual `d_syn - d_obs`, receiver-order time-reversed adjoint injection, discrete objective scaling by `dt`, CFL validation, and a finite-difference second-derivative gradient path with value-based tests.
- Reconstruction FWI now uses sign-correct residuals, `dt`-scaled objectives, validated wavefield timesteps and geometry, checkpointed adjoint replay, and a real encoded-gradient aggregation contract; the fabricated Hessian proxy was removed.
- The acoustic adjoint-state core is now single-sourced for L2 residuals, objective scaling, time reversal, and signed-correlation accumulation across seismic and reconstruction FWI.
- The acoustic GPU compute path now uses valid 8×8×4 workgroups, a 64-byte WGSL uniform layout, direct shape construction on readback, and a fused velocity update that removes three transient gradient arrays.
- The GPU profiling module is now publicly exported as `kwavers::profiling`, the obsolete tracker-injected GPU constructors were removed, and the current FDTD pressure roundtrip path is validated against the updated constructors.
- The vendored `k-wave-python` axisymmetric AS example parity slice is closed: `at_circular_piston_AS` now passes with `pearson_r=0.9907`, `psnr_db=27.98`, and `at_focused_bowl_AS` now passes with `pearson_r=1.0000`, `psnr_db=87.02`, after restoring Fortran-order sensor reshaping in the pykwavers comparison scripts.
- The beamforming delay-and-sum readback path now reconstructs the 3D volume without a zero-fill pass, handles `device.poll` results explicitly, and the k-space spectrum shift path now uses coercions instead of redundant casts.
- The FDTD solver now reuses staggered divergence scratch state instead of cloning a temporary divergence volume, the scalar dispatcher no longer performs a redundant full-volume zero-fill, and the GPU in-place overwrite path zeros only boundary faces before stencil writes.
- The electromagnetic FDTD boundary path no longer clones the full `EMFields` cache into the caller output on shape-compatible repeated use. `apply_em_boundary_conditions` applies PEC, recomputes the authoritative cell-centered cache through fixed `Ix4` cache views, copies values into existing caller buffers with `assign`, clears absent auxiliary fields, and validates computed interior E/H components plus pointer stability.
- The vendored `k-wave-python` example parity suite is closed for the 2-D FFT line-sensor example: `kspace_line_recon` is implemented natively, the non-square 2-D FFT axis ordering is corrected, and the parity metric now compares reconstructed images with `pearson_r ≈ 0.99999999`.
- The vendored `k-wave-python` example parity suite is closed for the 3-D planar-sensor time-reversal example: the forward pressure cache is reused, reconstructed fields are cached in the parity artifacts, and the slow k-Wave reconstruction path no longer runs on every test invocation.
- The vendored `k-wave-python` example parity suite is closed for the 3-D circular piston example: the pykwavers path now uses the native `KWaveArray` disc geometry, zeroes the PML halo before source-weight comparison against the padded reference mask, and matches the analytical on-axis steady-state profile with near-unity Pearson correlation.
- The vendored `k-wave-python` example parity suite is closed for the 3-D focused bowl example: the pykwavers path now uses the canonical spiral/BLI bowl source path, compares the physical-interior source weights against the k-wave-python reference, and validates the on-axis waveform parity with cached comparison artifacts.
- The vendored `k-wave-python` example parity suite is closed for the 2-D focussed detector example: the detector-averaged traces for the on-axis and off-axis source placements now match the k-wave-python reference, and the on-axis/off-axis energy ratio is parity-checked explicitly.
- The vendored `k-wave-python` example parity suite is closed for the 2-D sensor directivity modelling example: the source-angle trace matrix now matches the k-wave-python reference, and the derived directivity curve is parity-checked explicitly.
- The vendored `k-wave-python` example parity suite is closed for `na_controlling_the_pml`: the waveform attenuation sweep passes, and the generated k-Wave-style save-to-disk HDF5 input files match exactly across the reference and pykwavers outputs.
- The vendored `k-wave-python` example parity suite is closed for `at_array_as_sensor`: the Rust arc rasterizer now matches the upstream line-sampled BLI footprint with the correct `bli_tolerance = 0.05`, the parity bootstrap resolves the rebuilt `target/maturin/pykwavers.dll`, and the raw/combined detector comparisons now pass value-based regression checks.
- The vendored `k-wave-python` example parity suite is closed for `at_array_as_source`: the Rust/Python source matrix now preserves the canonical arc ordering, the example compares exact source-mask and distributed source-signal parity, and the p_max/p_rms field outputs now pass value-based regression checks.
- The pressure-source ordering contract is closed for the arc and linear-array builders: helper matrices now emit Fortran-order active-cell rows and exact source-matrix parity is pinned by slow regression tests.
- The vendored `k-wave-python` example parity suite is closed for `us_defining_transducer`: the pykwavers scan-line run now uses the k-Wave reference time-step count, the sensor-trace lengths align exactly, and the per-sensor parity report now passes value-based regression checks.
- The vendored `k-wave-python` example parity suite is closed for `ivp_photoacoustic_waveforms`: the cached initial-pressure traces now match the k-Wave and pykwavers outputs, the single-sensor waveform parity is validated directly, and the PASS report metrics remain intact.
- The vendored `k-wave-python` example parity suite is closed for `us_bmode_phased_array`: the quick steering-angle sweep now matches the cached k-Wave and pykwavers scan lines, the fundamental and harmonic B-mode images pass parity checks, and the GPU profile fields remain reportable.
- The vendored `k-wave-python` example parity suite is closed for `sd_focussed_detector_3D`: the per-source traces now match the k-Wave and pykwavers outputs, the on-axis/off-axis directivity ratio is parity-checked explicitly, and the PASS report metrics remain intact.
- The vendored `k-wave-python` `us_bmode_linear_transducer` example parity suite is now closed: the GPU scan-line path disables source-kappa correction to match the upstream transducer default, the host medium buffers are reused across scan lines with borrowed NumPy views, and the cached regression test now pins the PASS metrics contract.
- The pre-sweep `us_bmode_linear_transducer` GPU path now preserves the measured medium-upload timing after cached execution, reports per-line timings through a compact tuple-backed summary, and advances the lateral phantom window in place so successive scan lines do not rebuild the active slab from scratch.
- PSTD checkpointing is closed: save/resume is bit-exact, the checkpoint file is deleted after successful restore, and the regression example plus pytest coverage pin the PASS contract.
- `kwavers` no longer contains direct `rustfft` or `FftPlanner` usage in source, tests, or benches; all transform entry points route through Apollo-backed FFT APIs.
- The Apollo GPU FFT backend now dispatches radix stages with explicit pass boundaries and passes the 128³ kwavers parity check under a hybrid absolute/relative spectral metric.
- The neural beamforming layer adaptation path now uses a parameter-dependent calibration objective instead of constant-offset mutation.
- The distributed neural beamforming processor now partitions frame-major RF volumes across healthy processors with zero-copy frame views and deterministic recomposition.
- The sonogenetics physics slice now covers the 2026 bacterial-channel comparison by adding `MscLG22N` and `MscS` under the existing `MechanoChannel` enum, documenting two-state gating and reversal-current theorems, and validating channel-ordering plus current-sign contracts with value-semantic tests.
- The thermal property slice no longer permits negative acoustic absorption under ablation-range heating; `absorption_vs_temperature` now uses a positive exponential law with theorem/proof documentation and value-semantic tests across 37-100 °C. Generic soft-tissue sound-speed scaling now matches the coefficient used by the temperature-dependent medium model.
- The plasmonic effective-medium slice no longer uses linear dielectric placeholders for particle-volume mixing; dilute coupling now uses Maxwell-Garnett, quasi-static dense coupling now uses the physical Bruggeman root, and value-semantic tests verify closed forms, endpoints, and implicit-equation residuals.
- The gold plasmonics slice no longer uses a simplified Drude-Lorentz dielectric closure for `MieTheory::gold_in_water`; the implementation now uses Johnson-Christy tabulated `n,k` data, affine interpolation, exact complex-permittivity conversion, and value-semantic endpoint/interpolation tests.
- The electromagnetic plasmonic trait no longer embeds spheroid geometry formulas in the trait file or computes resonance from an invalid negative-permittivity square-root expression; geometry lives in a nested kernel file, and resonance/field enhancement use Fröhlich/Drude equations with finite damping and theorem tests.
- The sonoluminescence bremsstrahlung module no longer concentrates constants, Gaunt factors, Saha state solving, emission modeling, field assembly, and tests in one oversized file; the implementation is split into nested vertical modules with theorem documentation and the same value-semantic physics tests.
- The acoustic conservation module no longer concentrates energy, mass, momentum, entropy, intensity, heat-source, validation, and tests in one oversized file; each conservation law now lives in a nested vertical module with public facade re-exports and unchanged value-semantic tests.
- The sonogenetics channel module no longer concentrates constants, parameter records, channel identities, gating equations, current equations, and tests in one oversized file; each physics responsibility now lives under `channels/` with public facade re-exports and unchanged value-semantic tests.
- The quantum-optics module is no longer an orphaned oversized file; `physics::optics::mod` now declares and re-exports it, and constants, Einstein coefficients, Bessel/Gaunt kernels, correction assessment, and value-semantic tests live in a nested vertical tree. The prior constant `1.0` Gaunt-factor invalid-domain fallback and epsilon-denominator Einstein regularization are replaced with explicit `NaN` invalid-domain behavior.
- The skull aberration module no longer concentrates time-reversal theory, phase integration, element extraction, aperture-map assembly, and tests in one oversized file; each phase-screen responsibility now lives under `skull/aberration/`, and the element-correction API returns a typed dimension error for mismatched element coordinate arrays.
- The example suite now contains a real-input skull CT DICOM phase-correction path using RITK I/O, kwavers skull phase-screen physics, a 1024-element hemispherical array model, and value-carrying PPM/CSV outputs rather than a synthetic CT placeholder.
- The repository root no longer retains the obsolete scratch-artifact set of logs, text diagnostics, error captures, temporary patch scripts, scratch binaries, transient NumPy arrays, project-owned Python bytecode caches, stale PyO3 extension backups, or generated example output directories removed in the 2026-04-29 cleanup pass.
- The chemistry RK45 integrator no longer concentrates DOPRI5 coefficients, public API, error/stat types, RHS/species mapping, and tests in one oversized file; the module now uses a vertical directory tree with unchanged public facade exports and 4/4 targeted value-semantic integrator tests passing.
- The untracked `pykwavers/examples/output` parity figures and PML HDF5 comparison outputs generated by prior runs are removed; tracked validation images remain under version control and input datasets are preserved.
- The regenerated untracked `kwavers/examples/output` demo artifacts are removed after verification; source examples, input datasets, and checked-in validation assets remain preserved.
- The nonlinear Burgers analytical-solution module no longer concentrates the Bessel kernel, Fubini-Blackstock formulas, public facade, and tests in one oversized file; each responsibility lives in a child module with unchanged `burgers_equation` and `fubini_harmonic_amplitude` exports and 29/29 targeted nonlinear tests passing.
- Keller-Miksis thermodynamics no longer concentrates phase-change laws, Van der Waals EOS, vapor mass transfer, temperature ODE terms, and tests in one oversized file; each responsibility now lives under `keller_miksis/thermodynamics/` with unchanged facade exports and 32/32 targeted Keller-Miksis tests passing plus 1 pre-existing ignored equilibrium refinement test.
- CEUS microbubble dynamics no longer concentrates simulator configuration, Velocity-Verlet radial integration, nonlinear scattering efficiency, and tests in one file; each responsibility now lives under `ceus/microbubble/dynamics/` with unchanged parent `BubbleDynamics` export and 8/8 targeted CEUS microbubble tests passing.
- The Monte Carlo optical solver no longer concentrates parallel photon launch, atomic result assembly, MCML transport, Fresnel boundary handling, voxel geometry, and public solver facade in one oversized file; these responsibilities now live under `monte_carlo/solver/` with unchanged `MonteCarloSolver` exports and 20/20 targeted Monte Carlo optics tests passing.
- PCA multimodal fusion no longer returns `NotImplemented`; `FusionMethod::PCA` now dispatches to a covariance/principal-loading algorithm with shared registered-modality validation helpers and value-semantic tests for correlated modalities, dominant variance, confidence/uncertainty, and finite-input rejection.
- Enhanced BEM-FEM validation no longer returns `NotImplemented` for standard-BEM resonance screening or interface error estimation. The active diagnostic computes a Burton-Miller-aware frequency result, mesh-resolution residual estimate, adaptive refinement record, and condition-number estimate from validated configuration values; tests assert computed fields and rejection paths.
- PSTD/DG Chebyshev basis support no longer fails through the generic unsupported-basis branch. The active DG utilities now build Chebyshev `T_n` Vandermonde matrices, assemble derivative matrices from `T'_n=nU_{n-1}` with finite endpoint limits, and keep Fourier rejection specific to the missing periodic nodal contract.
- Simulation solver factory no longer reports `SolverType::KSpace` as not implemented. The assembly boundary now constructs `PSTDSolver` with `KSpaceMethod::FullKSpace`, propagates `SolverConfiguration` timing/order/GPU fields into concrete configs, and reports FEM as `FeatureNotAvailable` because the frequency-domain Helmholtz core requires a real Grid-to-TetrahedralMesh generator plus source/boundary assembly contract before it can satisfy the simulation trait.
- Discontinuous Galerkin simulation assembly no longer exits through the factory-level unavailable path for the core's valid layout. `simulation::solver_adapters::dg::DgSimulationSolver` owns the high-level adapter, validates `(grid.nx elements, grid.ny = p+1 nodes, grid.nz = 1 scalar)`, applies the existing modal/nodal DG transforms and time-stepper, rejects unsupported source projection explicitly, and records pressure-field/statistics results through the common `Solver` trait.
- The ultrasound physics book no longer lacks a non-genetic neuromodulation chapter parallel to HIFU, histotripsy, and BBB opening. Chapter 26 now defines the acoustic exposure, mechanochemical ion-channel, calcium, neural-response, Pennes thermal-dose, cavitation, and clinical-guidance contracts; the executable figure script emits six figure pairs plus `metrics.json`; manifest, README, and focused pytest coverage are synchronized.
- The ultrasound physics book no longer lacks the requested seismic full-waveform brain-imaging chapter. Chapter 27 now defines the CT-to-acoustic model, 1024-element helmet acquisition, simulated encoded ultrasound data, adjoint migration baseline, iterative FWI reconstruction contract, multi-slice reconstruction visualization, RITK-only CT loading path, generated RIRE CT figures, manifest/README links, and Rust value-semantic objective/contrast verification.
- The Chapter 27 resolution/contrast gap is closed for the bounded simulation artifact: the brain-helmet FWI path now uses a deterministic 1024-element hemispherical cap, 3-D source/receiver path lengths, CT-derived axial offsets, multiscale frequency continuation over 200/350/500/650/800 kHz, eight encoded receiver offsets, weak-Westervelt second-harmonic encoded rows, Sobolev-smoothed update conditioning, target-independent regularized FWI display output, twelve nonempty generated volume slices, centroid ROI visualization, and metrics showing the stack meets the visible-reconstruction contract.
- The Chapter 27 target-label ambiguity and attenuation gap is closed: `fig06` now separates raw `ct_hu` from the CT-derived acoustic `target_sound_speed_m_s`, and the sensitivity matrix includes CT-derived attenuation path integrals so skull/soft-tissue absorption changes the encoded simulated data.
- The Chapter 27 slice-wise inversion gap is closed: the brain-helmet path now resamples the RITK CT into an isotropic 3-D volume, builds CT-derived 3-D acoustic fields, runs one matrix-free 1024-element hemispherical-cap Born inversion over active brain voxels, exposes `run_seismic_helmet_fwi_volume_from_ritk_ct`, and slices the reconstructed 3-D arrays for `fig06` plus the centroid ROI artifact.
- The Chapter 27 3-D reconstruction-quality regression is closed for the bounded artifact: volume inversion now uses projected diagonal-preconditioned CG on the matrix-free normal equations, caches row norms and per-row constants across operator calls, adds stage-boundary Charbonnier edge-preserving proximal regularization, and `fig06` shows a target-independent mask-regularized FWI display row derived from the reconstructed 3-D volume. Regenerated metrics report global Pearson `0.856879060248954`, NRMSE `0.00264293344795594`, and stack slice Pearson range `0.801703760879262`-`0.879606457699549`.
- The Chapter 25 transcranial benchmark gap is closed for a bounded deterministic artifact: the existing CT-backed brain FUS workflow now exposes CT-conditioned skull-aware aperture selection, skull-ray phase/amplitude correction, skull-aware reference pressure, uncorrected baseline pressure, and TFUScapes-aligned relative-L2, focal-position, and max-pressure metrics through both `kwavers` and `pykwavers`. Remaining work is DeepTFUS neural surrogate training and TFUScapes-scale dataset generation, not benchmark plumbing.
- The Chapter 29 clinical-boundary gap is closed for public workflow ownership: same-device therapeutic ultrasound CT/anatomy/device orchestration now lives in `clinical::therapy::theranostic_guidance`, the solver-layer `theranostic` module is removed, PyO3 imports the clinical workflow path, and the book contract no longer identifies inverse solvers as the owner of clinical patient/device usage.
- The Chapter 29 inverse-kernel boundary gap is closed for the reduced same-aperture model: `solver::inverse::same_aperture` owns active-support graph indexing, finite-frequency active/passive row assembly, harmonic and ultraharmonic row assembly, deterministic noisy data handling, and graph-Laplacian PCG; `clinical::therapy::theranostic_guidance` owns CT/anatomy/device workflow, pressure exposure synthesis, and reporting.
- The Chapter 29 dense-row memory gap is closed for the reduced same-aperture model: active, passive, harmonic, and ultraharmonic channels now run through matrix-free `FiniteFrequencyOperator` implementations behind `LinearOperator`, while dense `RowMatrix` is retained only for equivalence tests and materialized diagnostics.
- The Chapter 29 over-claiming gap is closed for the current reduced model: public PyO3 naming, figure labels, metrics, and tests now identify the workflow as finite-frequency inverse plus source-encoded linear acoustic RTM, with explicit `is_full_wave_inversion = false` and `uses_nonlinear_wave_propagation = false` flags.
- The Chapter 29 noise-sensitive RTM residual gap is closed for the linear acoustic channel: receiver residual injection now defaults to a documented Charbonnier derivative with bounded influence, and metrics expose the selected misfit, robust scale, and objective value.
- The Chapter 29 nonlinear-FWI absence gap is closed for a bounded 3-D branch separate from the reduced linear workflow: `clinical::therapy::theranostic_guidance::nonlinear3d` owns CT-derived volume resampling, skin/calvarium same-aperture placement, deterministic focused source encodings, heterogeneous Westervelt FDTD propagation, the discrete adjoint for the implemented nonlinear recurrence, multiparameter `c/beta` gradients, CT/segmentation-derived target-ROI inversion masks, body-restricted `H1` regularization, Sobolev gradient smoothing, Rayleigh-Plesset period-doubling cavitation source generation, passive subharmonic nonnegative inversion, Rust-side nonlinear fusion scoring, PyO3 export, Figure 5 generation, and value-semantic Rust verification. Remaining work is thermoviscous/shock-capturing stabilization for higher histotripsy pressures and joint `c/alpha/rho/beta/bubble-density` inversion with higher-order robust trace misfits, not relabeling the linear RTM path.
- The Chapter 29 nonlinear cavitation data-alignment and allocator-pressure gap is closed for the separated Rayleigh-Plesset/passive block: simulated passive data is generated from the same active-voxel source vector used by the Green operator columns, the RP period-doubling observable uses an algebraically equivalent one-period ring buffer, and the passive inverse reuses prediction/residual/gradient workspaces across iterations.
- The Chapter 29 nonlinear FWI iteration allocator gap is closed for residual and line-search candidate storage: the source-encoded loop reuses one residual trace vector, and backtracking reuses one `LineSearchWorkspace` for candidate sound-speed and beta models instead of allocating full vectors for every scale.
- The canonical Westervelt FDTD finite-difference path no longer carries an incorrect 4th-order Laplacian stencil or an undocumented O6 gap. `solver::forward::nonlinear::westervelt` now applies the correct centered O4 coefficients, implements O6, returns typed validation errors for unsupported spatial orders, and reuses nonlinear/next-pressure workspaces during updates. Focused tests prove exact `laplacian(x^2+y^2+z^2)=6` on complete O2/O4/O6 stencils, preserve invalid-order configuration state, and verify pressure/nonlinear buffer pointer stability. (module-conflict gap from stale `wave.rs` and `ShiftPrior::Eq` conflict both closed 2026-05-17; see CHANGELOG.)
- The clinical imaging module no longer lacks dense and sparse ultrasonic speed-of-sound shift imaging. `clinical::imaging::reconstruction::sound_speed_shift` now reconstructs `delta c = c - c0` from differential travel-time shifts using the linearized straight-ray contract, exact segment/pixel intersection lengths, dense Tikhonov/H1 PCG, deterministic sparse row selection, and sparse L1 proximal reconstruction. Chapter 5 documents the approach and focused Rust tests pin forward sign, dense recovery, sparse localization, and invalid sampling rejection.
- The clinical speed-of-sound shift construction hot path no longer scales as `O(rows × active_pixels)`. Ray assembly now clips each segment to the image bounds, inserts exact parametric cell-boundary cuts, emits only crossed active cells, and preserves the previous per-cell intersection formula as a test oracle. Remaining extension work is 3-D curved-ray or finite-frequency sensitivity, not 2-D straight-ray assembly cost.
- The clinical speed-of-sound shift operator no longer concentrates construction, algebra, graph indexing, validation, and row storage in one flat file, and row storage no longer allocates one segment vector per selected measurement. `operator/{construction,algebra,graph,row_storage,validation}.rs` share one `SoundSpeedShiftOperator` SSOT, while `RayRowStorage` stores selected sample indices, row offsets, active columns, and segment lengths in flat arrays. Focused tests pin storage reconstruction and crossed-cell nonzero scaling.
- The clinical speed-of-sound shift solver no longer concentrates dense PCG, sparse ISTA, normal-equation application, objective evaluation, vector kernels, and scratch allocation in one flat file. `solver/{dense,sparse,normal,linear_algebra,workspace}.rs` share one solver contract, `SoundSpeedShiftWorkspace` owns reusable work vectors for repeated dense and sparse solves, and normal-diagonal assembly writes into caller-owned storage. Focused tests pin retained workspace capacity and equal reconstructed images across repeated calls.
- The clinical 2-D straight-ray speed-of-sound shift model no longer lacks a curved-array acquisition SSOT. `CurvedArray2d` defines circular-arc element coordinates, `CurvedArrayShiftScan` defines deterministic transmitter-major pitch-catch rows and measured-shift attachment, and the generated rows are ordinary `SoundSpeedShiftSample` values consumed by the existing straight-ray operator. Focused tests pin endpoint geometry, row order, invalid offset rejection, time-shift length rejection, and nonzero prediction from curved-array rows.
- The clinical speed-of-sound shift operator no longer assumes geometric straight-line sensitivity only. `ShiftPropagation::CircularArc` adds signed circular-arc propagation through exact subsegment grid traversal, and `ShiftSensitivity::FiniteFrequency` adds compact normalized Fresnel-tube voxel weights around straight or curved paths. The straight-ray/geometric default remains unchanged, and focused tests pin curved-path length, finite-frequency path-length conservation, off-axis sensitivity, and invalid model rejection.
- The clinical speed-of-sound shift repeated-frame path no longer rebuilds fixed acquisition geometry and operator rows for every frame. `SoundSpeedShiftPlan` owns the immutable samples, cached `SoundSpeedShiftOperator`, image shape, and reconstruction config; per-frame calls provide only a raw time-shift slice in original acquisition row order. Focused tests pin equivalence against direct reconstruction, invalid frame rejection, and curved-array curved-ray finite-frequency repeated frames with stable solver workspace allocation and stable cached weight count.
- The clinical speed-of-sound shift batch-frame path no longer requires callers to loop over frames and retain full objective histories for every reconstruction. `SoundSpeedShiftPlan::reconstruct_frames*` validates frame slices before solving, reuses one plan, one workspace, and one sampled-row RHS buffer, returns compact initial/final/iteration objective summaries by default, and retains full histories only under an explicit policy. Focused tests pin compact/full objective retention, invalid batch rejection, and direct single-frame equivalence.
- FEM structured-grid assembly is partially unblocked without fabricating time-domain semantics. `TetrahedralMesh::from_grid_vertices` converts a Cartesian vertex lattice to a conforming six-tetrahedra-per-cell mesh with exact volume preservation; `FemHelmholtzSolver::from_grid` uses that mesh for direct frequency-domain assembly; ILU/AMG preconditioner variants now fail explicitly instead of silently degrading to no preconditioner. Remaining simulation-level FEM work is source and boundary assembly, not geometry generation.
- FEM frequency-domain source and boundary assembly no longer requires direct mutation of private solver internals. The Helmholtz solver now exposes exact nodal load insertion, boundary-type Dirichlet queueing for tagged mesh nodes, and read-only RHS/mesh accessors. The source contract is intentionally nodal rather than nearest-node projection, so no hidden spatial approximation is introduced.
- FEM no longer lacks a simulation-layer frequency-domain backend contract. `FrequencyDomainAcousticBackend` separates steady-state complex-pressure Helmholtz solves from time-domain acoustic stepping, and `FemHelmholtzBackend` wraps the FEM core with pending nodal loads, structured-grid mesh assembly, solve, pressure interpolation, wavenumber reporting, and mesh-size reporting.
- Gaia mesh-provider integration no longer stops at an external crate presence. The root workspace now includes `gaia`, kwavers depends on it directly, and `TetrahedralMesh::from_gaia_indexed_mesh` imports real Gaia tetrahedral volume meshes with coordinate preservation, tetrahedral-cell validation, face-derived connectivity fallback for Gaia structured grids, explicit mathematical boundary-condition label mapping, conflict rejection, and volume-preservation tests. Remaining Gaia work is higher-level SDF/domain adapter selection and solver-factory wiring, not mesh data interchange.
- Transcranial aberration-correction validation no longer concentrates result typing, corrected-field simulation, interpolation, focal intensity, sidelobe, FWHM, and tests in one oversized file; these responsibilities now live under `aberration_correction/validation/` with unchanged public facade exports and 20/20 targeted aberration-correction tests passing. The FWHM metric now scans axis profiles directly instead of allocating three temporary vectors.
- Therapy cavitation no longer concentrates physical constants, detector type definitions, Blake threshold detection, spectral resonance thresholding, scalar cavitation metrics, and tests in one oversized file; these responsibilities now live under `therapy/cavitation/` with unchanged parent re-exports and 44/44 targeted therapy tests passing.
- Nonlinear harmonic generation no longer concentrates Fubini harmonic amplitudes, tissue harmonic imaging efficiency, optimal-frequency calculation, contrast-agent response, and tests in one oversized file; these responsibilities now live under `nonlinear/harmonics/` with unchanged public function exports and 29/29 targeted nonlinear tests passing.
- Skull heterogeneous properties no longer concentrate CT constants, skull layer types, model storage, binary-mask construction, CT/Hill construction, BVF/layer helpers, impedance lookup, and tests in one oversized file; these responsibilities now live under `skull/heterogeneous/` with unchanged public exports and 37/37 targeted skull tests passing.
- Bubble-field core no longer concentrates field storage, constructors, secondary Bjerknes coupling, adaptive time stepping, history recording, field accessors, statistics, and tests in one oversized file; these responsibilities now live under `bubble_field/core/` with unchanged `BubbleField` and `BubbleFieldStats` exports and 6/6 targeted bubble-field tests passing.
- Chemistry diffusion no longer concentrates solver types, grid helpers, species helper APIs, Crank-Nicolson step assembly, Thomas tridiagonal solve, and tests in one oversized file; these responsibilities now live under `chemistry/diffusion/` with unchanged public exports and 4/4 targeted diffusion tests passing.
- Keller-Miksis shape instability no longer concentrates shape-mode constants, state management, Plesset-Prosperetti time advancement, Blake wall-jet estimation, and tests in one oversized file; these responsibilities now live under `keller_miksis/shape_instability/` with unchanged public exports and 32/32 targeted Keller-Miksis tests passing plus 1 pre-existing ignored equilibrium refinement test.
- FEM variational boundary handling no longer concentrates public types, system validation, CSR mutation kernels, and tests in one flat file; these responsibilities now live under `domain::boundary::fem/` with unchanged facade exports, pre-mutation validation for matrix/RHS dimensions and boundary node domains, and 8/8 targeted FEM tests passing.
- Cloud deployment configuration no longer concentrates validated deployment, auto-scaling, monitoring, alert-threshold types, and tests in one flat file; these responsibilities now live under `infrastructure::cloud::config/` with unchanged public exports and cloud-feature diagnostics verified.
- Sonoluminescence detector and flexible-source calibration no longer expose flat oversized modules for constants/types/core calibration responsibilities; they now use nested facades with unchanged public exports and full kwavers lib verification passing.
- The ultrasound physics book no longer has only therapy, diagnostics, and theranostics chapters; `docs/book/` now has 20 domain chapters with reproducible SVG figures and module-linked validation contracts.
- Chapter 29 no longer renders same-device therapy/imaging platforms without CT-relative placement evidence. The active theranostic Rust module exports aperture-body clearance and skin-contact distance metrics for INSIGHTEC-like helmet and HistoSonics-like abdominal layouts; the PyO3 wrapper serializes those metrics; the figure script expands axes to include the complete aperture; tests assert nonzero helmet clearance and millimeter-scale abdominal skin coupling.
- MATLAB-free external benchmarking is now available through `external/k-wave-julia/benchmarks/kwavers`: KWave.jl provides native 1-D, 2-D, and 3-D reference traces, pykwavers runs matching active grids with singleton inactive axes for lower-dimensional cases, and the generated artifacts record solver time plus correlation, relative L2, max absolute difference, peak-ratio metrics, one-sample IVP timing alignment, sensor/PML placement, acceptance status, aligned pressure traces, residuals, scatter comparison, per-dimension timing plots, and aggregate dimension-sweep plots.

## Resolved Since Last Audit (2026-05-17) — Session 2

- Closed the `physics::book::wave` 500-line structural limit violation: `wave.rs` (641 lines)
  split into `wave/{mod,bessel,dispersion,linear,nonlinear,tests}`. Dead code eliminated:
  `bessel_j1` (double-sign bug in negative-x return path) and `bessel_j1_n` (normalisation
  bookkeeping error, annotated "not quite right" in original) both removed. Root cause was that
  `fubini_harmonic_amplitude` called the dead buggy `bessel_j1_n` instead of the clean
  Miller-recurrence `jn` driver. Fix routes `fubini_harmonic_amplitude` through `jn`, which uses
  Miller downward recurrence with two-buffer normalisation. All 6 resulting files ≤120 lines.

- Closed the `physics::book::cavitation` 500-line structural limit violation: `cavitation.rs`
  (586 lines) split into `cavitation/{mod,dynamics,histotripsy,power_spectrum,tests}`.
  All 12 cavitation tests preserved and pass in the new layout. Largest file: `dynamics.rs`
  (218 lines).

- Closed the `physics::book::rtm` 500-line structural limit violation: `rtm.rs` (526 lines)
  split into `rtm/{mod,backprop,beam,condition,temporal,tests}`. All 13 RTM tests preserved
  and pass. Largest file: `condition.rs` (142 lines).

- Closed the Westervelt test compile error: `absorption_causes_amplitude_decay_not_growth`
  directly assigned `medium.absorption = 5.0` (field is `pub(super)`); corrected to
  `medium.set_acoustic_properties(5.0, 1.0, medium.nonlinearity).unwrap()`. 5/5 Westervelt
  tests pass.

- Closed the imaging PSF test logic inversion: `compounding_narrower_than_single` asserted
  `psf4[0] > psf1[0]` but plane-wave compounding narrows PSF (lower value at off-axis point).
  Correct assertion: `psf4[0] < psf1[0]`. Math: eff_width_4 = 0.886·F#·λ/√4 = 0.665 mm;
  u₄(x=0.5mm) ≈ 0.752 → sinc²≈0.088; u₁ ≈ 0.376 → sinc²≈0.613.

- Full nextest run post-split: 4110/4113 PASS, 3 pre-existing 60 s timeouts
  (`fdtd_pstd_comparison::test_plane_wave_propagation`,
  `python_validation_integration_test::test_full_validation_suite`,
  `python_validation_integration_test::test_generate_ci_report`). No regressions.

## Resolved Since Last Audit (2026-05-17)

- Closed the Sobolev-smoother O(N·r³) performance gap in
  `solver::inverse::seismic::brain_helmet::volume_born::pcg::smooth_active_values_3d`:
  replaced the 3-D neighbourhood index scan with three separable 1-D prefix-sum
  box-filter passes (scatter → X filter → Y filter → Z filter → gather).  A
  parallel indicator array preserves the active-only averaging semantics.  For
  r=2 on a 56³ brain-FWI region the inner-loop work drops ~12×; the Z pass uses
  Rayon `par_chunks_mut`.  Removed the `ndarray::Array3` index table allocation.

- Closed the matrix-free LSQR absence gap: added
  `math::linear_algebra::iterative::lsqr::matfree` exposing `MatFreeOperator`
  trait and `solve_lsqr_matfree` (damped LSQR, Paige & Saunders 1982) over any
  operator pair `(matvec, t_matvec)`.  Added `ShiftPrior::Lsqr { damping }` to
  the sound-speed shift pipeline with `solver::lsqr::solve_shift_lsqr` wrapping
  `SoundSpeedShiftOperator` via `ShiftOperatorAdapter`.  For overdetermined
  systems LSQR converges without a normal-equation diagonal precomputation step.

- Closed the stale-monolith compiler-error gap: deleted `physics::book::cavitation.rs`
  (E0761 conflict with `cavitation/` split) and `physics::book::wave.rs` (already
  deleted; confirmed absent).  52/52 targeted module tests pass.

## Resolved Since Last Audit (2026-05-16)

- Closed the RTM 2-D-only physics gap in `physics::book::rtm`: added 3-D spherical
  Green's function `backprop_green_function_3d` (1/(4πr) amplitude, critical for
  correct migration weighting in 3-D transcranial RTM), source-normalised imaging
  condition `rtm_source_normalized_condition` (Guitton 2007: divides by forward
  field energy, removes skull-reflection amplitude bias vs plain cross-correlation),
  and `rtm_aperture_weighted_fusion` (per-element transmission-weighted fusion).
  All 8 new value-semantic tests pass.

- Closed the histotripsy lesion reconstruction analytics gap in
  `physics::book::cavitation`: added `mechanical_index` (FDA gating for cavitation
  nucleation), `inertial_cavitation_dose` (collapse-event Σ(R_max/R₀)³ dose
  metric), `histotripsy_lesion_radius_m` (energy-balance model R_L = R₀·(P₀·ICD/σ_y)^(1/3)
  from Rayleigh collapse PdV work), and `period_doubling_ratio` (subharmonic
  spectral ratio for passive cavitation monitoring). Fixed dead-code double
  assignment in `bubble_power_spectrum`. All 7 new tests pass.

- Closed the 1024-element helmet aperture uniformity gap in
  `nonlinear3d::aperture::brain_candidates`: replaced azimuth-only sort + 1-D
  stride selection with Fibonacci golden-angle sphere lattice
  `fibonacci_sphere_select`. Theorem: golden angle minimises max nearest-neighbour
  gap (Álvarez 2001); for 1024 elements this eliminates the prior azimuthal
  clustering that created grating lobes in RTM. Calvarium HU threshold lowered
  from 250 → 200 to include cancellous bone elements.

- Closed the synthetic brain phantom single-tissue gap in
  `synthetic::brain`: replaced uniform HU_BRAIN with five-class anatomy —
  gray matter cortex (HU 37), white matter (HU 28), CSF lateral ventricles
  (HU 10), thalamic nuclei (HU 38, stereotaxic histotripsy target). This enables
  correct sound-speed and attenuation variation for transcranial RTM/FWI validation.

## Residual Risks (2026-04-27)
- Full `kwavers` library tests pass, but `solver::forward::nonlinear::kzk::solver::tests::test_conservation_diagnostics_disable` exceeds the 60-second progress threshold. The next increment should profile the production KZK path and optimize it without reducing test coverage.
- Gold dielectric model extension remains open for non-Johnson-Christy datasets, particle-size corrections, and temperature-dependent optical constants; these require a typed dielectric-data strategy rather than new suffixed constructors.
- File-tree cleanup remains open for other oversized files. Closed 2026-04-30: Keller-Miksis validation → `validation/{mod,dynamics,thermodynamics,shape_stability}` (largest 144 lines); KZK solver → `solver/{mod,stepping,observables,conservation,traits,tests}` (largest 386 lines); FDTD solver → `solver/{mod,central_diff,construction,stepping,sources,accessors,gpu_accelerator,interface}` (largest 221 lines); Westervelt FDTD → `westervelt/{mod,laplacian,nonlinear,update,conservation,tests}` (largest 362 lines); clinical validation → `clinical/{mod,bmode,doppler,safety,score,report,tests}` (largest 264 lines); subspace beamforming → `subspace/{mod,music,eigenspace_mv,tests}` (largest 246 lines); `math::simd` → `simd/{mod,config,fdtd_ops,fft_ops,interpolation_ops,metrics,tests}` (largest 354 lines); staggered-grid operator → `staggered_grid/{mod,forward,backward,tests}` (largest 276 lines); beamforming traits → `traits/{mod,core,time_domain,frequency_domain,adaptive,config,tests}` (largest 148 lines); symplectic bubble integrator → `bubble_symplectic/{mod,stormer_verlet,yoshida,integrate,tests}` (largest 462 lines, source ≤191); linear elastography methods → `linear_methods/{mod,time_of_flight,phase_gradient,direct,volumetric,directional,tests}` (largest 151 lines); cylindrical medium projection → `cylindrical/{mod,construction,accessors,validation,tests}` (largest 344 lines, source ≤165); API models → `models/{mod,jobs,devices,imaging,clinical,dicom,mobile,tests}` (largest 265 lines); optical diffusion solver → `solver/{mod,construction,operator,preconditioner,solve,accessors,analytical,tests}` (largest 173 lines); Schwarz domain-decomposition boundary → `schwarz/{mod,gradient,transmission,boundary_impl,tests}` (largest 429 lines tests file, source ≤109); KWaveArray source → `kwave_array/{mod,math,construction,transform,geometry,bli_kernel,rasterizer_curved,rasterizer_planar,accessors,tests}` (largest source 319 lines, tests 437 lines) closed 2026-05-01; seismic FWI → `fwi/{mod,geometry,forward,adjoint,gradient,constraints,inversion,search,tests}` (largest source 404 lines, tests 262 lines) closed 2026-05-01. PINN 3D wave-equation solver → `solver/{mod,core,training,losses,collocation,ics,diagnostics}` (largest child ≤299 lines) closed 2026-05-01. Cavitation-coupled PINN domain → `cavitation_coupled/{mod,config,domain,construction,residuals,physics_domain,mie_scattering,tests}` (largest new file ≤220 lines) closed 2026-05-01. GPU PSTD time-loop → `time_loop/{mod,buffer,dispatch,encode,run}` (largest new file ≤230 lines) closed 2026-05-01. Top remaining oversized files: `solver/forward/fdtd/avx512_stencil.rs` (971 lines), `solver/inverse/pinn/ml/electromagnetic_gpu.rs` (966 lines), `clinical/imaging/reconstruction/real_time_sirt.rs` (950 lines), `solver/forward/pstd/gpu_pstd/pipeline.rs` (941 lines), `solver/inverse/pinn/ml/universal_solver.rs` (914 lines), `gpu/thermal_acoustic.rs` (903 lines), `solver/forward/elastic/swe/gpu.rs` (877 lines).
- KWave.jl versus pykwavers core propagation comparison is closed for the default no-PML 1-D/2-D/3-D IVP benchmark sweep: after correcting Julia 1-based center indexing, aligning KWave.jl initial IVP sample `0..n-2` against pykwavers sample `1..n-1`, and keeping the sensor outside PML, the runs pass with correlation/relative-L2 pairs of 1-D 0.999976/0.009421, 2-D 0.999948/0.014850, and 3-D 0.999909/0.019522. PML sweeps remain diagnostic-only unless the source and sensor are proven outside the absorbing layer.
- The reference-project benchmark matrix now includes k-wave-python execution for the same 1-D/2-D/3-D IVP sweep and links KWave.jl in the root README. k-wave-python passes against KWave.jl after applying its backend-specific two-sample IVP recorder alignment; MATLAB k-Wave remains source-present in `external/k-wave` but not runnable in this environment because neither MATLAB nor Octave is available.
- PSTD lower-dimensional runtime now skips singleton-axis y/z FFT derivative passes in the Cartesian propagator, and Apollo 3-D real-to-complex transforms no longer allocate a full temporary complex volume before filling caller-owned output. This is algebraically equivalent for embedded 1-D/2-D grids because length-1 axes have only the zero spectral mode, and it reduces memory traffic for every 3-D real FFT. Latest rebuilt pykwavers benchmark remains PASS and now beats k-wave-python in 1-D (pykwavers 0.011985 s vs k-wave-python 0.025742 s) while still lagging in 2-D (0.064855 s vs 0.039046 s) and 3-D (0.682185 s vs 0.116092 s). The remaining gap is active-axis FFT lane movement and PSTD setup overhead, not benchmark thresholds.
- PSTD IVP initialization now also skips y/z velocity inverse FFT setup for singleton axes. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.009733 s vs k-wave-python 0.018814 s, 2-D 0.052291 s vs 0.008122 s, and 3-D 0.686284 s vs 0.109846 s. 1-D is faster; 2-D/3-D remain open and should target active-axis FFT lane movement with per-worker scratch or a true 2-D/3-D plan strategy rather than shared mutex plan reuse.
- Apollo 3-D FFT non-contiguous axis passes now use a flat lane workspace instead of nested per-lane vectors, reducing allocation count while preserving the separable DFT pass order. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.010804 s vs k-wave-python 0.019047 s, 2-D 0.053316 s vs 0.038922 s, and 3-D 0.508495 s vs 0.086710 s. 3-D improves materially from the prior 0.686284 s, but active-axis lane movement and PSTD setup overhead remain open because 2-D/3-D still trail k-wave-python.
- Lower-dimensional PSTD inactive-axis bookkeeping now avoids full-array derivative zero-fills and no-op split-density updates for singleton Cartesian axes. This is algebraically equivalent because the inactive spectral derivative is zero and the stored split-density component remains unchanged in the pressure sum. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.009833 s vs k-wave-python 0.018980 s, 2-D 0.047745 s vs 0.028957 s, and 3-D 0.505348 s vs 0.114158 s. A tested parallel gather/scatter variant for Apollo lane movement was rejected after regression to 3-D 0.537763 s.
- PSTD IVP setup now reuses solver-owned `div_u` scratch for the exact source-kappa velocity scale instead of allocating a separate full-volume scale array. This preserves the closed-form `Δt/(2ρ₀)·sinc(acos(κ_src))` initialization and is safe because `div_u` is scratch until the first density/pressure update. Final pre-commit sweep remains PASS with pykwavers 1-D 0.009152 s vs k-wave-python 0.018000 s, 2-D 0.049823 s vs 0.005937 s, and 3-D 0.516947 s vs 0.093414 s. Remaining 3-D debt is active FFT lane movement or a more direct active-dimensional FFT strategy.
- PSTD construction now reuses the `k_mag` allocation for `source_kappa` after absorption initialization, eliminating one full-volume setup allocation while preserving `source_kappa = cos(c_ref·dt·|k|/2)`. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.009874 s vs k-wave-python 0.018412 s, 2-D 0.045697 s vs 0.035149 s, and 3-D 0.480546 s vs 0.096624 s.
- PSTD IVP construction now reuses `div_u` as the initial-density output buffer and splits from it directly into the three density components, removing the transient `rho_init` allocation while preserving `rho = rhox + rhoy + rhoz`. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.009789 s vs k-wave-python 0.015888 s, 2-D 0.044440 s vs 0.040324 s, and 3-D 0.410568 s vs 0.079913 s.
- PSTD homogeneous material setup now fills `rho0`, `c0`, and `BonA` from the single canonical medium sample when `Medium::is_homogeneous()` is true, eliminating per-voxel coordinate conversion and dynamic medium-property dispatch for homogeneous benchmarks while preserving stored material fields. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.008503 s vs k-wave-python 0.020747 s, 2-D 0.044713 s vs 0.035048 s, and 3-D 0.473107 s vs 0.089178 s.
- PSTD pressure-source diagnostics now parse source time-shift and source-gain environment controls once at solver construction instead of once per pressure-source step. The invariant is per-instance deterministic source control: changing the environment after construction no longer mutates a running solver, and the hot path avoids repeated environment lookup and parse work. Latest rebuilt sweep remains PASS with pykwavers 1-D 0.009737 s vs k-wave-python 0.012629 s, 2-D 0.045513 s vs 0.027969 s, and 3-D 0.417537 s vs 0.079591 s.
- PSTD disabled trace logging no longer triggers a full pressure-field `max_p` scan. The scan now runs only when TRACE is enabled, preserving diagnostic semantics while removing O(N) memory traversal from default production stepping. Fresh release rebuild now passes after `.cargo/config.toml` selects Clang/Clang++ and Ninja for native `openjpeg-sys` and `charls-sys` compilation; the Windows override loader resolves the GNU stable-ABI `libpython3.dll` import from the active interpreter. Rebuilt comparison sweep PASS with KWave.jl/pykwavers/k-wave-python timings of 1-D 0.801664/0.009561/0.016715 s, 2-D 1.380449/0.047431/0.032788 s, and 3-D 2.206667/0.473963/0.085284 s.
- Apollo is updated to `6ba31de`; `kwavers::math::fft` now re-exports Apollo's current root cache API plus `fftfreq`/`rfftfreq`/`fftshift`/`ifftshift`, and PSTD/k-space wavenumber vectors route through Apollo `fftfreq` instead of duplicated local index-to-bin formulas. Verification remains PASS: Apollo `fftfreq` tests 9/9, PSTD numerical tests 6/6, `cargo check -p kwavers`, `cargo build -p pykwavers --release`, and rebuilt comparison sweep with KWave.jl/pykwavers/k-wave-python timings of 1-D 0.854880/0.009644/0.016240 s, 2-D 1.585750/0.055903/0.006481 s, and 3-D 2.346329/0.495308/0.099361 s.
- Plane-wave compounding, Helmholtz FEM solver, conservative interpolation, and elastic 2-D PINN model no longer depend on flat modules. They now live in vertical directory modules with facade re-exports and focused tests: `plane_wave_compounding/{mod,config,compound,tests}`, `fem/solver/{mod,config,core,tests}`, `interpolation/conservative/{mod,mode,interpolator,tests}`, and `elastic_2d/model/{mod,network,tests}`. Verification remains PASS: `cargo check -p kwavers`, plane-wave 8/8, FEM solver 3/3, conservative interpolation 5/5, and default non-`pinn` model selector 0/0.
- BEM boundary conditions no longer live in a flat module. The `domain::boundary::bem` facade now owns only module documentation and re-exports, while `manager.rs`, `types.rs`, and `tests.rs` hold implementation, enum state, and value checks. Verification remains PASS: `cargo check -p kwavers`; BEM boundary tests 5/5.
- DAS-PAM delay-and-sum no longer lives in a flat module. The `analysis::signal_processing::pam::delay_and_sum` facade now owns only algorithm documentation and re-exports, while `processor.rs`, `types.rs`, and `tests.rs` hold implementation, serializable configuration/event state, and value checks. Verification remains PASS: `cargo check -p kwavers`; DAS-PAM tests 7/7.
- Transducer hardware interface no longer lives in a flat module. The `infrastructure::device::transducer_interface` facade now owns only architecture documentation and re-exports, while `hardware.rs`, `manager.rs`, `mock.rs`, `types.rs`, and `tests.rs` hold the trait boundary, registry, simulation implementation, command/status data, and value checks. Mock calibration no longer sleeps for simulated hardware latency. Verification remains PASS: `cargo check -p kwavers`; transducer-interface tests 12/12.
- Diverging-wave configuration no longer exists as a duplicate orphan under an ignored directory. The active `domain::sensor::ultrafast::diverging_wave` module now uses `diverging_wave/config.rs` as the single `DivergingWaveConfig` source, `processor.rs` for `DivergingWave`, and `tests.rs` for value checks; facade exports remain unchanged.
- Kuznetsov solver responsibilities no longer live in one flat solver file. `solver/{wave,rhs,model_impl,diagnostics_impl}` now separates state construction, RHS assembly, trait integration, and conservation diagnostics; RHS evaluation reuses workspace `k1` and structured tracing replaces direct console output.
- Seismic FWI misfit functions no longer live in one flat file. The active `misfit` directory separates dispatch types, norm metrics, envelope/phase Hilbert-transform metrics, and Wasserstein CDF metrics while preserving `MisfitFunction` and `MisfitType` exports.
- GPU k-space acceleration no longer lives in one flat file. The active `gpu::kspace` facade re-exports `KSpaceGpu` and `KspaceShiftGpu`, with propagation and spectral-shift responsibilities isolated in `kspace_gpu.rs` and `shift_gpu.rs`.
- FDTD-FEM coupling no longer has an ambiguous flat-file and directory module pair. The active hybrid coupling tree separates configuration, interface mapping, Schwarz coupling, solver orchestration, and tests under `fdtd_fem_coupling/`.
- GPU-feature diagnostics no longer report stale unused imports from the active `kwavers` crate after the k-space and FDTD-FEM splits; remaining warnings are emitted by the path dependency `ritk-io`.
- FDTD k-space correction no longer lives in one flat file. The active tree isolates spectral operators and their value tests under `kspace_correction/`, preserving the `KSpaceFdtdOperators` facade export.
- Beamforming localization search no longer lives in one flat file. The active tree separates localization policy types, search orchestration, and value tests under `beamforming_search/`, preserving the existing public re-exports.
- A broad flat-module segment is now closed across analysis, clinical, domain, GPU, infrastructure, math, profiling, solver, and validation modules. Each converted module now uses a parent `mod.rs` facade and SRP child modules for types, configuration, implementation, orchestration, and tests while preserving import paths validated by `cargo check -p kwavers`.
- CPML profile computation no longer lives in one flat file. The active `domain::boundary::cpml::profiles` tree separates profile state/facade, collocated/staggered kernel construction, and CPML value tests. Narrowband integration tests no longer live in one flat file; shared deterministic fixtures, pipeline checks, invariance checks, snapshot-consistency checks, and steering-unit checks now live in separate child modules.
- Fusion integration tests no longer live in one flat file. The active `physics::acoustics::imaging::fusion::tests` directory now separates basic fusion, confidence/uncertainty, tissue-property, registration/quality, and advanced probabilistic/non-rigid scenarios while preserving the same `#[cfg(test)] mod tests` parent module and value-semantic assertions.
- Plane-wave compounding no longer exposes a mock-like workflow compatibility accessor for thermal-acoustic configuration. `PlaneWaveCompound::config()` now depends on the actual plane-wave geometry and produces a thin-volume `ThermalAcousticConfig`; tests assert the derived dimensions, spacings, sound speed, and CFL time step.
- PSTD/DG Legendre differentiation no longer evaluates the removable endpoint singularity in `P'_n(x)=n(P_{n-1}-xP_n)/(1-x²)` at GLL nodes. The implementation now uses the analytic endpoint limits, the DG trait facade no longer carries a stale stub/reconstruction label, and focused tests verify finite matrix entries plus exact derivative action on constant and linear nodal polynomials. Chebyshev and Fourier DG bases remain explicit unsupported paths until implemented through the same matrix contract.
- Spectral filtering no longer returns `NotImplemented` for real-space anti-aliasing. The active filter performs `u_f = FFT⁻¹(HxHyHz·FFT(u))` with Apollo-owned 3-D FFT plans, validates cutoff/domain inputs, preserves constant fields exactly, removes rejected Nyquist modes, and supplies the `SpectralOperator::apply_antialias_filter` hook used by pseudospectral derivative operators.
- Spectral filtering no longer forces hot callers through an extra owned real-field copy. The active filter exposes `apply_into` with caller-owned complex spectrum and real output workspaces; `apply` delegates to that path, uses `output` as FFT staging storage, and preserves the same tensor-product modal transfer contract.
- Pseudospectral derivative evaluation no longer clones x/y/z line algorithms or allocates a complex `Array1` per pencil. The active derivative operator uses one const-generic axis kernel and caller-owned line/output workspaces, while `derivative_x/y/z` remain allocating convenience wrappers over the same implementation.
- DG Fourier basis algebra no longer returns `NotImplemented`. The active basis and differentiation builders implement real Fourier trigonometric columns and analytic derivatives for valid periodic nodes on `[-1,1)`, and reject duplicated periodic endpoints before matrix inversion. Full solver-level Fourier DG remains intentionally rejected at `DGSolver::new` because the current constructor always supplies GLL nodes containing both periodic endpoints; a periodic-node constructor is the remaining implementation boundary.
- Complex linear-algebra extension no longer returns `NotImplemented` for the Hermitian eigendecomposition domain. `LinearAlgebraExt<Complex<f64>>::eig()` delegates to the canonical complex Hermitian Jacobi solver, returns zero-imaginary eigenvalues as complex scalars for trait compatibility, verifies `Av=λv` residuals in tests, and rejects non-Hermitian matrices instead of fabricating a general nonsymmetric eigensolver.
- MUSIC localization no longer returns `NotImplemented` at the `LocalizationProcessor` time-delay boundary. Because the trait lacks covariance snapshots, the active adapter now delegates the arrival-time-only case to the canonical TDOA Newton least-squares solve and documents the domain split: `MUSICProcessor::run` remains the covariance/subspace API, while `localize` performs the physically defined hyperbolic timing solve.
- Analytical phase shifting no longer declares strategies that `PhaseShifter::apply_phases` cannot execute. `Focused` delegates to the spherical focal law, `MultiFocus` accepts flat 3D focal-point chunks and delegates to the equal-weight multipoint law, and `Custom` validates a full element-phase vector before wrapping/optional quantization. Tests compare strategy dispatch against the canonical direct methods and reject malformed targets.
- Multimodal image fusion no longer returns `NotImplemented` for maximum/minimum intensity projection. The active `intensity_projection` module implements pointwise order-statistic fusion over already-registered modalities, rejects shape mismatches, records selected-modality confidence and uncertainty, and preserves result metadata. PCA and deep-learning fusion remain explicit unsupported paths pending eigenspace/model-specific design.
- A second broad module-tree segment is closed across time-domain DAS beamforming, clinical ULM, CEUS ultrasound imaging, domain boundary/sensor/signal, math inverse problems, acoustic simulation, elastic SWE, FDTD SIMD, Helmholtz Born series, GPU PSTD tests, PINN elastic/ML components, and sonoluminescence benchmarks. The converted modules now use directory-backed facades and child modules for types, configuration, kernels, processors, tests, and validation constants without changing parent module imports.
- Multi-rate time integration no longer relies on compile-only placeholder tests. Analytical tests now pin RK4, Adams-Bashforth, CFL, diffusion, and subcycling contracts; `MultiRateController` now guards `ceil(global_dt/component_dt)` against machine-roundoff integer-boundary overestimation.
- Time-scale separation no longer clones every component slice before derivative analysis. `TimeScaleSeparator::analyze` now passes `ArrayView3` directly, uses one monomorphized central-difference contract for x/y/z first and second derivatives, and handles domains without an interior central stencil without unsigned-index underflow.
- Multiphysics field coupling no longer clones full source fields for acoustic-to-optical, optical-to-thermal, or acoustic-to-thermal read paths. The active coupler uses const-generic disjoint field borrows, validates field set shape compatibility before mutation, and reuses strong-coupling previous-state buffers after the initial convergence snapshot.
- Functional sparse-kernel and windowed operators no longer require cloneable field/kernel/output values for read-only traversal. `apply_kernel` and `apply_kernel_parallel` keep borrowed nonzero kernel entries, the parallel path derives coordinates from flat Rayon indices without a coordinate buffer, and `windowed_operation` passes `ArrayView3` directly instead of owning each window.
- Functional transform smoothing no longer clones the full input field before updating interior voxels. The active smoothing transform computes one output array from borrowed input cells, preserves boundaries by direct scalar copy, and uses const-generic axis-neighbor helpers for the six directional stencil reads. The general transform pipeline no longer requires `Clone` for owned values that are consumed by each stage.
- Functional field operations no longer require cloneable elements for read-only traversal over `Array3<T>`. The active `par_map_field` maps flat Rayon indices directly to `(i,j,k)` coordinates and avoids materializing a `Vec<&T>` before parallel work dispatch.
- Time-reversal signal reversal no longer clones the full sensor-time matrix before swapping each row. `TimeReversalUtils::time_reverse_signals` now constructs the output directly from the analytical index involution `t -> n_t - 1 - t`, preserving the same public API with one output allocation.
- The documentation set no longer lacks a book entry point for the requested therapy/diagnostics/theranostics split. `docs/book/` now contains separate chapters with theorem/proof/algorithm sections and committed SVG figures, and the README no longer depends on a remote placeholder image.
- The `math::simd` FDTD pressure path no longer advertises an AVX-512 implementation while falling back to AVX2. The AVX-512F path now uses 512-bit loads, arithmetic, and stores over 16 row-contiguous interior cells with scalar cleanup, and tests validate exact FDTD recurrence semantics.
- Another flat-module segment no longer concentrates unrelated responsibilities in single files. The active tree now uses facade modules with SRP children for ML uncertainty, beamforming test utilities, layer validation, lithotripsy fracture, boundary coupling/CPML, CT loading, signal helpers, GPU orchestration, API types, AWS cloud lifecycle, photoacoustic acoustics/reconstruction, multiphysics solving, GPU pipeline management, BEM-FEM coupling, KZK harmonic tracking, GMRES, and PINN ML support. Azure/GCP provider files are removed because they were incomplete stubs and are represented only as documented future extension points.
- Clinical mechanical-index calculation no longer accepts undefined mathematical domains. The active code validates finite positive center frequency, finite nonnegative attenuation, finite nonnegative focal depth, finite nonnegative maximum depth, and at least two depth samples before computing `MI = P_r / sqrt(f_c)` or constructing a depth profile. The former non-semantic panic-only test is replaced by analytical attenuation monotonicity and invalid-domain assertions.
- Sixth-order central-difference no longer has a split-tree facade gap. The active `central_difference_6/` directory now contains `mod.rs`, `core.rs`, and `tests.rs`, so the differential parent imports one directory-backed module and the deleted flat monolith remains superseded.
- Apollo WGPU FFT no longer accepts a power-of-two-only validation surface while advertising arbitrary 3D transforms. The f32 and f16 Bluestein kernels now precompute `FFT(h)` for the chirp convolution, launch flat padded workgroups, address `N`-length stages with `row * M + local` workspace indexing, conjugate before and after the forward reduction for inverse DFT, and apply inverse-axis `1/N` scaling. kwavers now validates Apollo WGPU FFT against Apollo CPU spectra and non-power-of-two round trips through reusable buffers.
- GPU-feature clippy no longer fails on active kwavers GPU modules. Remaining GPU cleanup from this cycle is source-level and mechanical: PSTD documentation indentation, integer dispatch helpers, buffer readback borrowing, FDTD slice sizing, transfer-queue sorting, and fast-nearfield facade completion.
- FEM boundary assembly no longer mutates CSR matrices before proving the algebraic system domain. The active manager validates square/matching stiffness and mass matrices, matching RHS length, in-range boundary nodes, finite Robin coefficients, and finite Sommerfeld wavenumber; tests now assert actual CSR row contents and unchanged RHS on rejection.
- Clinical safety no longer treats thermal index as documentation-only. The active safety facade exports a dedicated thermal-index module with TIS/TIB/TIC model labels, explicit model reference power, finite-domain validation, power attenuation derating, and value tests for exact ratio calculation, derating, statuses, and invalid domains.
- Generated-output cleanup is active: stale demo logs under `kwavers/examples/output` should be removed during normal workspace hygiene.
- RTM no longer contains a catch-all `NotImplemented` branch for supported imaging-condition enum variants. `EnergyNormalized`, `SourceNormalized`, and `Poynting` now evaluate input-sensitive formulas against source/receiver wavefields, and targeted tests assert the computed image values. A further broad facade-consolidation segment is closed across active analysis, clinical, core, domain, GPU, infrastructure, math, simulation, and solver modules with unchanged public module paths validated by `cargo check -p kwavers`.
- KZK zero-pressure propagation no longer executes the full FFT-backed Strang split for a mathematically invariant state. The active stepper detects exact zero complex pressure, advances counters, preserves conservation diagnostics, and returns the same zero field. Warm focused verification for `test_conservation_diagnostics_disable` is 1.97 s with `CARGO_INCREMENTAL=0`.
- PSTD shader ABI comments no longer duplicate a second buffer-layout authority inside the WGSL source. The shader now points to `docs/gpu/pstd_shader_abi.md` for bind-group, shift, twiddle, source, and invariant contracts while retaining only local comments needed to audit dispatch bodies. Current shader line count is 844.
- GPU-feature PSTD shader verification no longer fails before reaching the shader ABI test because of stale facade imports. The active construction module resolves `bgl` and `bind_groups` as sibling pipeline modules, `DelaySumGPU` is exported from the delay-and-sum facade, and GPU-gated unused imports are removed from active kwavers code.
- Sensor-recorder non-staggered velocity recording no longer materializes whole collocated velocity fields before sampling sparse sensors. The active method evaluates the same half-cell backward interpolation at each sensor coordinate, eliminating transient full-grid allocations for `ux_non_staggered`, `uy_non_staggered`, and `uz_non_staggered` while preserving boundary ghost-cell semantics.
- Pressure-statistics single-field extraction no longer allocates an aggregate `SampledStatistics` record when callers request only one field. Dedicated `sample_p_max`, `sample_p_min`, `sample_p_rms`, and `sample_p_final` methods now serve narrow accessors, while `sample_at_positions` composes those methods for the aggregate API. Zero-step sampled RMS is defined as zero, consistent with the full-field RMS contract.
- Velocity-statistics allocation no longer treats any requested component statistic as a request for all three component accumulators. `SensorRecordSpec` carries per-component stat predicates, `SensorRecorder::with_spec` allocates only the requested component grids, and narrow sampled component extractors preserve access when the aggregate three-component stats record is intentionally unavailable.
- Sensor-recorder time-series extraction no longer forces every consumer through owned `Array2` clones. The active simple recorder now provides borrowed views for full allocated pressure/velocity buffers and recorded prefixes, so internal callers can avoid O(n_sensors·n_steps) copies while the legacy owned extractors remain available for API compatibility.
- Solver-level sensor-data extraction no longer requires callers to reach into `SensorRecorder` or clone through owned extraction APIs when a borrowed read is sufficient. PSTD, FDTD, and elastic SWE solver facades now forward full-buffer and recorded-prefix `ArrayView2` pressure/displacement views from the recorder.
- Recorder checkpoint extraction now has an explicit borrowed contract. `checkpoint_state_view` returns the recorded pressure prefix plus recorder counters without allocation; owned checkpoint state and PSTD persistence compose that view at the point where serialized ownership is required.
- Intensity-average recording no longer aliases velocity-sample need with velocity-buffer need. `records_ux/uy/uz` now describes stored velocity time series, while intensity recording uses pressure plus instantaneous velocity fields directly. Average-only intensity requests allocate running sums without `ux/uy/uz` or `Ix/Iy/Iz` time-series buffers.
- Sensor-recorder velocity module resolution is no longer ambiguous. The completed `simple/velocity/` directory is the compiled module root, separating time-series views, intensity accumulation, component stats, recording kernels, and tests while preserving the parent `SensorRecorder` facade.
- Sampled pressure and velocity statistics no longer allocate a temporary `Vec` before creating `Array1` results. The active samplers allocate the final array once and fill it by sensor index, reducing transient heap traffic for repeated sparse sensor-stat extraction.
- Time-averaged acoustic-intensity extraction now supports caller-owned output buffers. Repeated diagnostics can call `fill_i_avg_x/y/z` to avoid per-read allocation while receiving explicit errors for unrequested components or wrong output length.
- Sampled pressure-statistics extraction now supports caller-owned output buffers. Repeated diagnostics can call `fill_p_max/min/rms/final` to avoid per-read allocation while receiving explicit errors for unrequested statistics or wrong output length.
- Sampled velocity-statistics extraction now supports caller-owned output buffers. Repeated diagnostics can call `fill_ux/uy/uz_{max,min,rms}` to avoid per-read allocation while receiving explicit errors for unrequested component statistics or wrong output length.
- GPU PSTD run-cache buffer packing no longer uses ambiguous placeholder terminology for the zero-sensor binding case. The active invariant is a named non-empty storage-buffer sentinel required by WebGPU, with dispatch counts proving shaders do not consume the sentinel when the logical count is zero.
- PSTD anti-aliasing verification no longer uses a success-only `is_ok()` assertion. The active regression validates the spectral filter contract by attenuating the Nyquist checkerboard mode under the configured Butterworth filter and checking finite output.
- Velocity-only recording no longer drops samples when pressure storage is disabled. `SensorRecorder::record_step` advances `next_step` after statistics and optional pressure sampling, and `record_velocity_step` then writes the velocity-only sample at `next_step - 1`; the regression asserts no pressure buffer is allocated and `ux` is still recorded.
- CPML per-dimension configuration tests no longer assert only success/failure. They now verify exact valid-axis thickness/alpha values, exact invalid-dimension error messages, and the closed-form dimension-specific reflection coefficient derived from the documented Collino-Tsogka exponential attenuation formula.
- Microbubble state tests no longer treat successful construction as sufficient verification. They now pin constructor constants for SonoVue/Definity/drug-loaded states, gas/drug closed-form quantities, and exact validation-error parameter/reason pairs for invalid constructor inputs and mutated runtime state.
- CentralDifference2 tests no longer rely on success-only construction and failure checks. They now verify exact numerical error payloads for invalid spacing and insufficient grid dimensions, and assert that both boundary and interior stencils differentiate linear fields exactly.
- CentralDifference4 tests no longer rely on success-only construction and failure checks. They now verify exact numerical error payloads for invalid spacing and insufficient grid dimensions, and assert that first-order boundary, second-order near-boundary, and fourth-order interior stencils differentiate linear fields exactly.
- CentralDifference6 tests no longer rely on success-only construction and failure checks. They now verify exact numerical error payloads for invalid spacing and insufficient grid dimensions, and assert that first-order boundary, second-order near-boundary, fourth-order near-boundary, and sixth-order interior stencils differentiate linear fields exactly.
- Staggered-grid tests no longer rely on success-only construction and failure checks in the active operator module. They now verify exact numerical error payloads for invalid spacing and insufficient grid dimensions across forward/backward x/y/z allocation paths, plus the forward-x zero-allocation path.
- Differential operators no longer retain a tracked stale staggered-grid draft implementation. The active module graph exports only `staggered_grid`, and `rg` finds no references to `staggered_grid_draft_20260430172431`.
- Analysis no longer retains tracked stale adaptive-beamforming or clinical-validation draft implementations. The active module graph exports `adaptive::{mvdr,music,subspace}` and `validation::{clinical,theorem_validation}`, and `rg` finds no references to the removed draft trees.
- Functional-ultrasound atlas loading no longer returns a uniform test placeholder while claiming Allen atlas semantics. The active default is an explicitly documented analytical stereotactic mouse phantom with nonzero annotation labels; production Allen CCF data remains an external input to `with_annotation`, and registration uses a borrowed reference image.
- Functional-ultrasound vasculature no longer returns `NotImplemented` for static vessel classification, centerline extraction, or Doppler velocity calculation. The active module now performs input-sensitive geometric classification, centerline extraction, and explicit Doppler-equation velocity estimation while rejecting static-segmentation velocity requests that lack Doppler/tracking data.
- Functional-ultrasound vasculature no longer owns a parallel Otsu implementation. Threshold selection delegates to `ritk_core::segmentation::threshold::otsu`, leaving RITK as the single thresholding authority and retaining local code only for vasculature-specific connected-component analysis.
- Pykwavers PSTD extraction no longer clones full recorder matrices before trimming the initial recorder column. The active path borrows recorder prefixes through `ArrayView2`, drops `t=0` from the view when present, and allocates only the final Python-owned output array.
- Analytical phase shifting no longer rejects supported strategy variants through a catch-all `NotImplemented` path. Strategy dispatch now maps `Linear`, `Focused`, `MultiFocus`, and `Custom` to distinct input contracts and value-carrying phase laws, while steering-angle and focal-distance validation use the documented SI constants consistently across shifter, beam steering, and dynamic focusing.
- Medium construction no longer silently erases requested heterogeneous file-backed inputs. `MediumBuilder` preserves the scalar heterogeneous path for explicit scalar configs, but `tissue_file` and `property_maps` now fail with a typed feature-availability error until a real volume-loader boundary is provided.
- CEUS orchestration no longer reports an unregistered default factory as an unfinished implementation. The domain-level `CEUSOrchestrators` registry now documents that concrete CEUS engines must be supplied by an assembly layer and returns `FeatureNotAvailable` when no default factory is registered.
- Hybrid solver validation no longer uses a mock convergence curve or unavailable solver/reference/eigenvalue branches. The active suite validates a documented manufactured acoustic eigenmode through a sixth-order finite-difference residual, exact second derivative, monotone grid-refinement contraction, and CFL calculation independent of concrete solver assembly.
- Comparative examples no longer rely on text metrics alone for parity review. The shared parity utility now writes side-by-side PNG panels with reference, pykwavers, and signed-difference views; mask, velocity, PR FFT/TR, initial-pressure, phase, and canonical comparison scripts now emit visual artifacts; a utility test scans comparative examples for a declared visual export marker.
- Initial-pressure parity no longer compares PML-contaminated late arrivals as if they were direct-wave parity. The active example derives the admissible comparison window from grid spacing, sound speed, PML size, and sensor margin, then records PASS/FAIL plus visual traces for that physical interval.
- IVP particle-velocity parity no longer compares a k-wave-smoothed source against a raw pykwavers source. The plot residual analysis traced the discrepancy to implicit k-wave `smooth_p0=True`; the active example now shares one Blackman-smoothed `p0` and disables k-wave double smoothing.
- HIFU procedure visualization is now backed by a real acoustic-thermal model rather than a schematic plot. The example computes a focused aperture field, converts intensity to absorbed heat with `Q = 2 alpha I`, advances Pennes bioheat with an explicit stability check, and verifies focus and temperature behavior through value-semantic tests.
- HIFU cavitation feedback no longer stops at a mechanical-index map or a terminal sample-and-hold thermal source. The active example integrates a Keller-Miksis bubble model, converts bubble volume acceleration to passive receiver pressure, derives receiver activity from subharmonic/RMS emissions and Rmax/R0 inertial-onset tracking, repeats receiver-control bursts through the sonication, and feeds the nonconstant squared relative-pressure envelope into Pennes heat deposition with value-semantic regression coverage.
- Burn DAS beamforming tests no longer accept any `Ok(Array3)` with the expected shape. The active tests assert computed beamformed amplitudes for known sensor distances, apodization weights, CPU wrapper output, and multi-focus all-one RF data, so constant or zero-output mocks fail.
- DeepFusion multimodal image fusion no longer returns a `NotImplemented` error for valid registered modalities. The active implementation is a parameter-free voxel-attention operator: robust salience normalization supplies the attention likelihood, modality quality and configured weights supply the prior, softmax normalization yields convex weights, and optional uncertainty is the normalized attention entropy. Trained U-Net/Transformer fusion remains a separate future extension requiring validated model weights and datasets.
- Acoustic-intensity record fields are no longer metadata-only. The active recorder computes `p·u` time series and time averages at sensor positions, and pykwavers exposes the corresponding k-Wave record strings and result attributes.
- Acoustic-intensity dependency semantics are now test-pinned and documented: requesting x-intensity requires pressure and ux only, not unrelated velocity components or statistic accumulators, and the pykwavers record-mode table lists the complete intensity mapping.
- DAS-PAM no longer has a Rust/Python boundary split where Python callers must clone data into owned Rust matrices before the authoritative beamformer can run. The active `DelayAndSumPAM::beamform_view` accepts borrowed input views, validates sensor count, grid shape, nonempty samples/candidates, and finite values before indexing, and uses fractional-delay interpolation instead of rounded sample delays. The PyO3 `passive_acoustic_map_das` binding reuses that contract. Python comparison coverage lives with the other pykwavers examples at `pykwavers/examples/passive_acoustic_mapping_compare.py`; it now writes 2-D parity panels and a 3-D cavitation-volume detection comparison via `xy`/`xz`/`yz` maximum-intensity projections. The latest regenerated images show numerical agreement with the independent NumPy delay-law reference at normalized L2 `3.34e-16` for 2-D and `1.65e-16` for 3-D.
- Histotripsy examples no longer live only as book figure generation. The active pykwavers example `pykwavers/examples/histotripsy_cavitation_compare.py` computes a Rayleigh-Sommerfeld focused-aperture volume, maps it to intrinsic-threshold cavitation probability and millisecond-pulse Keller-Miksis collapse strength, estimates adiabatic bubble internal temperature at van der Waals hard-core collapse, writes five comparison PNGs including the pressure-response curve, and reports classical `P(cav)>=0.5` volume `0.060 mm^3`, ms-pulse `Rmax/R0 >= 1.8` onset volume `243.468 mm^3`, half-maximum focal support `0.800 mm` lateral by `5.400 mm` axial, and peak collapse gas temperature `3.50e6 K`. A row-major volume-lift bug that smeared radial samples across depth is fixed by using tiled z-indices for NumPy's fastest-varying z axis; bulk `100 C` tissue temperature is no longer used as a cavitation gate. `pykwavers/tests/test_histotripsy_example.py` pins zero-pressure bubble response, monotone collapse strength, finite normalized maps, focal peak alignment, and axial/lateral support bounds.
- Acoustic-intensity memory policy no longer conflates instantaneous velocity-field dependency with raw velocity time-series storage. Intensity modes keep driving velocity sampling for `p·u` while avoiding `ux/uy/uz` output-buffer classification unless those velocity records are requested directly.
- Recorder intensity verification no longer contains presence-only assertions or stale average-intensity record names. The active tests inspect matrix dimensions, zero initial average state, computed intensity values, and explicit absence contracts, and in-tree docs consistently use `I_avg_x/I_avg_y/I_avg_z`.

## Session 2026-05-04 Gap Closures

- **CLOSED** [patch] `ScratchArena` trait absent — no shared contract existed for pre-allocated solver scratch buffers. Fix: defined `ScratchArena` in `solver::workspace`; implemented for `SolverWorkspace` (with formula correction: was `2×complex_size + 3×real_size`, correct is `1×complex_size + 3×real_size`), `KuznetsovWorkspace` (14 buffers, `SpectralOperator` excluded), and `BornWorkspace`; deleted 2 orphaned files; wired `validation/contract.rs`; value-semantic tests added for all implementors. `cargo check` clean, targeted tests pass.
- **CLOSED** [patch] Checkpoint test `match + panic!` control flow — 2 arms used `panic!` instead of `let…else`, hiding pattern-match failures as undifferentiated panics. Fix: replaced with `let KwaversError::InvalidInput(ref msg) = err else { panic!(...) }` and explicit message-content assertions.
- **CLOSED** [patch] R2C/C2R FFT wiring in PSTD — audit confirmed `forward_r2c_into`/`inverse_c2r_into` from apollo-fft are correctly called with `nz_c = nz/2 + 1` for all pressure/velocity/absorption stages. No gap, no changes required.
- **CLOSED** [patch] Spectral-CPML incompatibility silent no-op — enabling CPML with `kspace_correction = Spectral` silently bypassed boundary corrections. Fix: guard in `enable_cpml` returns `KwaversError::InvalidInput`; `# Errors` doc + Roden & Gedney (2000) reference added; 2 value-semantic tests verify rejection (Spectral) and acceptance (None) with `cpml_boundary.is_some()` assertion.

## Session 2026-05-04 (continued) Gap Closures

- **CLOSED** [minor] 3D CPU DAS beamformer absent — all `#[cfg(not(feature = "gpu"))]` paths in `analysis::signal_processing::beamforming::three_dimensional::processing` returned `FeatureNotAvailable`.  Fix: implemented `delay_and_sum_cpu` in `cpu/das.rs` (plane-wave receive delay, linear interpolation, Rayon parallelism over voxels, full apodization support); created `cpu/mod.rs`; added `pub(super) mod cpu;` to `three_dimensional/mod.rs`; replaced stubs in `processing.rs` for `process_volume`, `process_streaming`, `validate_input`, and `process_delay_and_sum`.  Theorem (Thomenius 1996, Jeong & Kwon 2013) and proof in file header.  4 value-semantic tests: zero-delay passthrough, channel-mismatch error, coherent gain M (co-located), exact receive-delay geometry (τ = 1 sample at sz=3 mm).
- **CLOSED** [minor] 3D CPU MVDR beamformer absent — no CPU adaptive beamforming existed.  Fix: implemented `mvdr_cpu` in `cpu/mvdr.rs` with spatially-smoothed covariance (Shan & Kailath 1985), relative diagonal loading R_δ = R + δ·(tr(R)/L)·I, Cholesky/LU solve via nalgebra, Rayon parallelism over voxels; replaced `process_mvdr_3d` stub in `processing.rs`.  Theorems (Capon 1969, Synnevåg et al. 2007) and proofs in file header.  5 value-semantic tests: L=1 identity proof, L=1 δ-invariance, channel-mismatch error, subarray-exceeds-array error, diagonal-loading positive-definiteness over 4 decades of δ.
- **CLOSED** [patch] `BeamformingProcessor3D::new` CPU path — constructor returned `FeatureNotAvailable` unconditionally; now returns a valid processor struct without GPU for use with CPU dispatch path.

Total: 3258/3258 tests PASS (9 net-new beamforming tests added).

## Remaining Open Gaps

- **PARITY SWEEP 2026-05-06 (post time-axis fix, commit 2c793a2f)** — re-ran 18 representative `examples/*_compare.py` scripts with fresh pykwavers caches:
  - **Bit-for-bit numerical equivalence (Pearson 1.000000, PSNR ≥ 300 dB):** `ivp_homogeneous_medium`, `ivp_loading_external_image`, `ivp_opposing_corners_sensor_mask`.
  - **High-quality PASS (Pearson ≥ 0.985):** `ivp_3D_simulation` (r=0.985, PSNR 50.6 dB), `na_modelling_absorption` (r=1.000000, PSNR 90.3 dB), `tvsp_homogeneous_medium_monopole` (r=0.992).
  - **PASS without explicit Pearson capture (alternate metrics):** `sd_focussed_detector_2D`, `sd_focussed_detector_3D`, `tvsp_transducer_field_patterns`.
  - **Pre-existing OOB panic — CLOSED (5 scripts):** `ivp_1D_simulation`, `ivp_heterogeneous_medium`, `ivp_photoacoustic_waveforms`, `ivp_recording_particle_velocity`, `ivp_binary_sensor_mask`. Root cause: `TrilinearInterpolator::interpolate` clamped per-axis index to `grid.{ny,nz} - 2`, which underflows in release builds when an axis has extent 1 (`1usize - 2 = usize::MAX − 1`); the clamp became a no-op and `field[[i, j+1, k]]` read index 1 of a shape-1 axis → `pyo3_runtime.PanicException: ndarray: index out of bounds`. All 5 scripts route through `pkw.Medium(sound_speed, density)` (heterogeneous constructor, trilinear interpolation enabled by default) on quasi-1D / quasi-2D grids with `ny == 1` or `nz == 1`. Fixed by collapsing degenerate axes inside the interpolator (zero fractional offset, next-index = base-index). Two analytical regression tests added (`interpolate_quasi_1d_grid_does_not_panic_on_degenerate_y_z`, `interpolate_quasi_2d_grid_does_not_panic_on_degenerate_z`). Post-fix sweep: `ivp_heterogeneous_medium` PASS r=0.9999 PSNR 56 dB; `ivp_binary_sensor_mask` PASS r=1.000000 PSNR 303 dB (bit-for-bit); `ivp_photoacoustic_waveforms` PASS r=1.000000 PSNR 70 dB (after script-level fix to apply k-wave's default `smooth_p0=True` to the source); `ivp_recording_particle_velocity` PASS (alternate metric format — sweep regex didn't capture); `ivp_1D_simulation` r=1.000 per-sensor / global rms_ratio=0.82 (waveforms match, slight heterogeneous-interface amplitude mismatch — physics-precision residual, not a bug). 2831/2831 kwavers lib tests pass (+2 new degenerate-axis trilinear tests).
  - **Timeout (>300 s):** `na_modelling_nonlinearity` — needs longer budget or perf optimization.
  - **Output-format-mismatch (`?` in regex sweep, may have passed):** `na_source_smoothing`, `na_controlling_the_pml`, `tvsp_homogeneous_medium_dipole` — re-run individually with custom regex if needed.
- **CLOSED** [patch] pykwavers sensor-data time-axis was shifted by `−dt` relative to k-wave-python's. `Simulation::trim_initial_recorder_sample` and `trim_initial_recorder_view` were dropping column 0 (the `t = 0` initial sample) of the recorder buffer, leaving columns `[p(dt), p(2·dt), …, p(Nt·dt)]` exposed at indices `[0, 1, …, Nt − 1]`. k-wave-python returns `[p(0), p(dt), …, p((Nt − 1)·dt)]`. Empirical verification: shifting pykwavers output by `+1` sample produced perfect cross-correlation `r = 1.000000` with k-wave-python on `na_modelling_absorption_compare.py`. Fix slices `[skip..time_steps]` instead of `[start..]` so the `t = 0` initial state is preserved and the redundant post-final-step value is dropped. Result: `na_modelling_absorption_compare.py` Pearson 0.917 → **1.000000**, PSNR 35.3 → **90.3 dB** (FFT round-off floor); `ivp_homogeneous_medium_compare.py` PSNR 36.6 → **304 dB**; `ivp_loading_external_image_compare.py` PSNR 41.6 → **302 dB** (bit-for-bit numerical equivalence to k-wave-python). 2829/2829 kwavers lib tests pass; 6/6 pykwavers Rust unit tests pass; the two existing `trim_initial_recorder_*` contract tests were updated to assert the new k-Wave-aligned semantics.
- **CLOSED** [major] CPU PSTD power-law absorption operator was dimensionally wrong vs. k-Wave C++/MATLAB and the GPU WGSL shader, producing effectively zero attenuation at realistic configurations. Fix ports the GPU WGSL `absorb_pressure_correction` shader and k-wave-python `kspace_solver.py:613` formulation to CPU: `p += c² · (τ·L1 − η·L2)` with `L1 = IFFT(|k|^(y−2)·FFT(ρ₀·∇·u))` and `L2 = IFFT(|k|^(y−1)·FFT(ρ_total))`, no `Δt` factor, applied algebraically inside `update_pressure`. Velocity divergence is recomputed locally because `apply_pressure_sources` clobbers `dpx` between density and pressure updates. Quantitative analytical test added pinning the dispersion term at peak-relative tolerance 1e-9. Verification: `na_modelling_absorption_compare.py` peak now matches k-wave-python to 4 sig figs (0.3836 vs 0.3836); α-attenuation sweep matches across α ∈ [0.25, 10.0] dB/(MHz^y cm) to 4–5 sig figs in dB; `ivp_loading_external_image_compare.py` PASS (Pearson 0.9997, RMS 0.996, PSNR 41.6 dB) — the 28% RMS drift the prior session had filed as a separate gap was the same operator bug. 2829/2829 kwavers lib tests pass. Residual Pearson 0.917 in the 1-D delta-IVP script is a 1-dt propagator phase offset (independent of α), not an absorption defect.
  - **Evidence (1-D, NX=1024, dx=12.5 µm, dt=2.5 ns, c=1500, α=0.25 dB/(MHz^y cm), y=1.5):**
    - k-wave-python: 0.5mm → 1.5mm peak attenuation = **−3.38 dB** (peak 0.384 → 0.260)
    - pykwavers PSTD: 0.5mm → 1.5mm peak attenuation = **+0.16 dB** (peak 0.491 → 0.500, no decay)
  - **Mechanism:** `PSTDSolver::apply_absorption` in `kwavers/src/solver/forward/pstd/physics/absorption/apply.rs` integrates `rho_α += dt · (τ · IFFT(nabla1 · FFT(∂u_α/∂α)) − η · IFFT(nabla2 · FFT(∂u_α/∂α)))` per axis on **split density**, with no `ρ₀` inside the FFT and no `c²` outside. The GPU WGSL shader `absorb_pressure_correction` in `kwavers/src/gpu/shaders/pstd.wgsl` (already correct, matches k-Wave C++ `sumPressureTermsLinear`) applies `p += c² · (τ · L1 − η · L2)` instantaneously to **pressure**, with `L1 = IFFT(nabla1 · FFT(ρ₀ · div_u_total))` and `L2 = IFFT(nabla2 · FFT(rho_total))` and **no `dt` factor** because absorption is an algebraic correction, not a running integral.
  - **Magnitude of CPU error:** missing `(ρ₀ / dt) ≈ 4 × 10¹¹` plus per-axis vs. total div_u shape mismatch. The aggregate effect at the test configuration is order-of-magnitude vanishing absorption.
  - **Why existing CPU tests passed:** `test_fft_based_absorption_reduces_amplitude` only asserts `max_abs > 0.0`, and `test_fft_absorption_energy_dissipation` asserts `final_energy < initial_energy`. Both are satisfied by a 10⁻¹¹ correction. No quantitative attenuation test against `α(f) = α₀ f^y` exists. These are exactly the cosmetic-compliance tests forbidden by the standards rule.
  - **Confirming script:** `pykwavers/examples/na_modelling_absorption_compare.py` at α=0.25, y=1.5 reports `peak_ratio = 1.30, rms_ratio = 1.41, pearson_r = 0.873`. Setting α=0 in the same setup yields `peak_ratio = 1.000124, rms_ratio = 1.0015, pearson_r = 0.857` (the residual Pearson 0.857 is a 1-sample timing offset that does not affect amplitudes) — propagator and source loading are correct, the entire amplitude residual is the absorption operator.
  - **Fix scope (deferred — multi-file, requires re-verification across multiple parity scripts):** port the GPU shader's pressure-side formulation to CPU (build full `div_u = dpx + dpy + dpz`, compute `L1 = IFFT(nabla1 · FFT(ρ₀ · div_u))` and `L2 = IFFT(nabla2 · FFT(ρ_total))` once per step in `update_pressure`, apply `p += c² · (τ · L1 − η · L2)`); remove the per-axis density-side `apply_absorption` loop; replace cosmetic existence/energy tests with a quantitative attenuation test that pins peak decay vs. analytical `exp(−α₀ ω^y x)` for a Gaussian pulse over a measured distance; re-verify `na_modelling_absorption_compare.py`, `ivp_loading_external_image_compare.py`, and any other α>0 parity script.
- Validate pykwavers comparison metrics: re-run `canonical_comparison.py` and update `metrics.csv` with current Pearson/PSNR values.
- Axisymmetric solver validation: `Geometry::Axisymmetric` flag is wired into `PSTDConfig`/`FdtdConfig` and the WSWA-FFT AS propagator is implemented; end-to-end parity vs. k-Wave axisymmetric examples pending.
- Pykwavers no longer carries a stale acoustic-intensity mapping assertion that treats intensity as raw `ux` storage. The Python-facing record-mode regression now pins the same split as kwavers: pressure storage plus instantaneous velocity sampling, without `ux` time-series allocation.
- Book chapters `acoustic_propagation.md`, `cavitation_and_bubbles.md`, `elastography.md`, `sources_and_transducers.md`, `media_and_tissue_models.md` remain at skeleton depth (1-2 KB) and require full theorem/proof/figure expansion.

## Closed Since 2026-05-10 (elastic-PSTD consolidation + KWave.jl parity)

- **CLOSED** [arch] Architectural redundancy between `pstd::PSTDSolver` (acoustic-fluid pseudospectral) and `solver/forward/elastic_wave.rs::ElasticWave::update_wave` (acoustic-fluid pseudospectral with μ=0 hard-coded — a literal duplicate sitting in the elastic module tree). Diagnosis: the duplicate's spectral primitives `update_stress_in_place` / `update_velocity_in_place` were genuinely general elastic (μ correctly entered both passes), but the orchestrator on top forced μ=0 and ran an acoustic stepper. Fix: deleted the duplicate orchestrator + the wrapping `ElasticWavePlugin` + the dead `PhysicsModelType::MechanicalStress` factory variant (no production caller); moved the spectral primitives into `pstd::extensions::PstdElasticPlugin` with a new orchestrator `pstd::extensions::ElasticPstdOrchestrator` driving them through the canonical PSTD step loop. The acoustic-fluid limit (μ → 0) is now a documented theorem rather than a duplicate code path.

- **CLOSED** [arch] Cross-engine elastic-2D parity vs KWave.jl `pstd_elastic_2d`. Final state: `peak_ratio = 1.0000` across 4 downstream sensors, `pearson_mean = 0.974`, RESULT = PASS in `pykwavers/examples/ewp_elastic_2d_jl_compare.py --pstd`. Three sequential root-cause fixes, each verified against the convergence trace:
  1. Stress accumulation contract (was overwriting → now adds increment to persistent stress).
  2. Staggered-grid k-shift via `i·k·exp(±i·k·Δ/2)` complex spectral derivative operators (matches KWave.jl `ddx_k_shift_pos/neg`); was off by ~3× peak with collocated derivatives.
  3. Source dimensionality (3-D y-line at one z-slice → 2-D y-z plane via mask through all z); was off by ~3× peak from cylindrical-vs-line spreading.
  Convergence trace: legacy FD 0.36 → collocated PSTD 0.71 → staggered PSTD 0.78 → staggered + 2-D source 0.974.

- **CLOSED** [patch] `ElasticWaveSolver::extract_recorded_displacement_components` velocity-vs-displacement naming bug. Root cause: `record_velocity_step` is called with `current_field.{vx,vy,vz}` (particle velocity), but the legacy public-accessor name said "displacement", which triggered a `np.gradient(_, dt)` "displacement→velocity conversion" in `external/elastic_julia_parity/compare_elastic.py` that produced acceleration → a 10⁷ peak_ratio artifact. Fix: renamed to `extract_recorded_velocity_components` with full theorem-and-proof Rustdoc (proves the recorded trace equals `vx(i,j,k,t_n)` by tracing through the recorder + the velocity-Verlet integrator); deprecated the misleading old method as a back-compat alias; repaired the historical comparison script to drop the spurious `np.gradient` call. Matched-mode peak_ratio: 10⁷ → 1.63 (with FD path) → 1.0000 (with new PSTD orchestrator).

- **CLOSED** [minor] KWave.jl parity gap for physics not covered by k-wave-python examples — 5 `*_jl_compare.py` scripts: bioheat 1D, elastic 2D, time-reversal 2D, phased-array 3D, beamforming 2D. All PASS.

- **CLOSED** [patch] Phase-control allocation and elastic-PML constructor gap. Diagnosis: `PhaseShifter` allocated a scratch phase array and cloned it into `phase_offsets` for every direct phase-law call, and flat multifocus dispatch reboxed packed target triples before delegating to the same law; elastic real-space and split-field PML constructors carried parallel 10-argument surfaces; the split-field orchestrator used `is_some()` followed by `unwrap()`; `elastic_orchestrator/split_field_step.rs` exceeded the 500-line structural invariant. Fix: reuse `phase_offsets` as the owned workspace, validate multifocus targets before state mutation, consume packed targets through slices, introduce `ElasticPmlSpec` as the shared PML construction contract, replace unwrap control flow with joint option matching plus typed error for impossible split-PML state, and trim non-semantic separator lines to bring the file below 500 lines. Verification: phase-shifting tests pass 15/15, elastic-orchestrator tests pass 15/15, kwavers lib check and clippy pass, and the source line-count audit reports no `kwavers/src/**/*.rs` file above 500 lines.

- **CLOSED** [patch] Thermal diffusion source/perfusion allocation and stencil-duplication gap. Diagnosis: `ThermalDiffusionPlugin` cloned a full-volume heat-source field before every solver update, `PennesBioheat::update` allocated a full perfusion array even though the perfusion source is point-local, and `ThermalDiffusionSolver::calculate_laplacian` duplicated second- and fourth-order stencil loops while lower-dimensional grids required per-axis admissibility. Fix: use borrowed `ArrayView3` sources through the solver and bioheat APIs, compute perfusion directly inside the parallel temperature update traversal, and replace the duplicated Laplacian loops with one const-generic `ORDER` body plus const-generic axis selectors. Verification: thermal diffusion tests pass 6/6, kwavers lib check and clippy pass, and the source line-count audit reports no `kwavers/src/**/*.rs` file above 500 lines.

- **CLOSED** [patch] Hyperbolic thermal diffusion workspace and axis-duplication gap. Diagnosis: `CattaneoVernotte` stored three previous-flux arrays that were assigned every step but never read, allocated a fresh divergence array during every temperature update, and cloned x/y/z centered-difference logic across component-specific loops. Fix: remove `prev_flux_x/y/z`, add an owned divergence workspace used by `update_temperature`, and route heat-flux plus divergence component math through const-generic axis selectors. Verification: hyperbolic tests pass 2/2 with analytical one-step `T(i)=i²` temperature, workspace pointer stability, owned-vs-workspace divergence equivalence, and boundary-zero assertions; thermal diffusion tests pass 6/6; kwavers lib check and clippy pass; source line-count audit reports no `kwavers/src/**/*.rs` file above 500 lines.

- **CLOSED** [patch] PSTD Dirichlet-PML bypass x-plane allocation gap. Diagnosis: `apply_pml_to_velocity` and `apply_pml_to_density` saved bypass x-planes by cloning every selected yz-plane into `Vec<Array2<f64>>` for ux/uy/uz and rhox/rhoy/rhoz on each PML application. Fix: add a solver-owned `(bypass_rows, ny, nz)` scratch buffer, resize it when bypass rows are configured, and route both velocity and density bypass preservation through one helper that validates scratch shape, saves selected rows, applies the PML mutation, and restores rows even on mutation error. Verification: bypass helper tests pass 3/3; PSTD-filtered lib tests pass 88/88; kwavers lib check and clippy pass; source line-count audit reports no `kwavers/src/**/*.rs` file above 500 lines; propagator scan finds no `to_owned()` in the velocity/pressure PML bypass files.

- **CLOSED** [patch] KZK real-field diffraction FFT allocation gap. Diagnosis: the AS PSTD backlog item was already closed in active code (`AsContext` uses preallocated WSWA buffers and cached `Fft2d` plans), but KZK `AngularSpectrum2D::propagate` and real-field `KzkDiffractionOperator::apply_with_step` still allocated complex fields and allocating forward/inverse 2-D FFT results per call. Fix: add cached `Fft2d` plans and reusable `Array2<Complex64>` scratch buffers to both operators, keeping the same diagonal spectral propagators while changing storage to in-place FFT/multiply/IFFT. Verification: angular-spectrum tests pass 17/17; parabolic-diffraction tests pass 5/5; KZK-filtered lib tests pass 43/43 with 3 pre-existing long validations ignored; kwavers lib check and clippy pass; source line-count audit reports no `kwavers/src/**/*.rs` file above 500 lines.

- **CLOSED** [patch] Monolithic residual field-block allocation gap. Diagnosis: `MonolithicCoupler::compute_residual` materialized each stacked state block with `slice(...).to_owned()` and allocated a new rate array for every physics-rate branch. Fix: introduce `field_block_view` for borrowed block access, route residual coupling reads through `ArrayView3`, make the Laplacian kernel storage-polymorphic with caller-owned output, and reuse one residual rate scratch buffer. Verification: monolithic tests pass 15/15; kwavers check and clippy pass; touched monolithic source files are 344/278/324 lines.

- **CLOSED** [patch] Monolithic previous-state clone gap. Diagnosis: `MonolithicCoupler::solve_coupled_step` cloned the flattened Newton state into `u_prev` on every coupled step even when dimensions were unchanged. Fix: add `u_prev_scratch`, take a shape-compatible workspace from the solver, refresh it with `assign`, and return it after unflattening. Verification: monolithic tests pass 16/16; kwavers check and clippy pass; clone scan finds no `u_current.clone()` in `coupler.rs`; the touched coupler file is 416 lines.

- **CLOSED** [patch] Monolithic Newton RHS/update allocation gap. Diagnosis: each non-converged Newton iteration allocated a full-state RHS through `&f * -1.0`, then allocated another full-state temporary for `du * step_size` and a replacement `u_current`. Fix: add solver-owned `rhs_scratch`, fill it with `-F(u)` before GMRES, and apply `u += alpha * du` in place. Verification: monolithic tests pass 17/17; kwavers check and clippy pass; scan finds no old RHS/update allocation expressions in `coupler.rs`; the touched coupler file is 484 lines.

- **CLOSED** [patch] Monolithic line-search candidate allocation gap. Diagnosis: backtracking line search allocated `u + alpha * du` for every candidate alpha, and `coupler.rs` had reached 484 lines with inline tests, blocking additional solver-state documentation under the structural limit. Fix: move coupler tests into `monolithic/coupler/tests.rs`, add solver-owned `line_search_state_scratch`, and overwrite the trial state for each alpha while restoring scratch ownership on residual-evaluation errors. Verification: monolithic tests pass 18/18; kwavers check and clippy pass; scan finds no `let u_new` or `du * alpha` allocation pattern in line search; touched monolithic files are 355/294/181 lines.

- **CLOSED** [patch] Monolithic JVP perturbed-state allocation gap. Diagnosis: every GMRES Jacobian-vector product allocated `u + eps*v`, then allocated a scaled difference for `(&F(u+eps*v) - &F(u)) / eps`. Fix: add solver-owned `jvp_state_scratch`, build the perturbed state in place, and reuse `F(u+eps*v)` as the returned JVP by subtracting `F(u)` and scaling in place. Verification: monolithic tests pass 19/19; kwavers check and clippy pass; scan finds no old `let u_plus`, `v * eps`, or scaled-difference JVP allocation pattern; touched monolithic files are 365/313/226 lines.

- **CLOSED** [patch] Monolithic line-search configuration and verification-unblock gap. Diagnosis: `NewtonKrylovConfig::line_search_parameter` was documented and tested as `(0,1]` but not used by `line_search`, so simulation callers could not bound Newton step length; verification was also blocked by root workspace missing the `ritk-*` inherited dependencies required by `ritk-io` and by a duplicate `BinaryThreshold` import in `ritk-core`. Fix: line search now uses `alpha_k = alpha_max / 2^k`, rejects invalid `alpha_max` with `KwaversError::Validation(InvalidValue)`, root `workspace.dependencies` declares the required local `ritk-*` path dependencies, and `ritk-core` has one `BinaryThreshold` re-export. Verification: monolithic tests pass 21/21; kwavers check and clippy pass clean.

- **CLOSED** [patch] Monolithic solve input-contract gap. Diagnosis: `solve_coupled_step` flattened fields before validating `dt`, Newton bounds, empty field maps, or field/grid shape equality; empty maps fabricated a `(1,1,1)` state and shape mismatches could reach ndarray copy panics. Fix: add a pre-flattening `validate_solve_inputs` gate with typed validation errors and O(field count) cost. Verification: monolithic tests pass 25/25; kwavers check and clippy pass; invalid-call tests assert exact errors and unchanged solver state.

- **CLOSED** [patch] Monolithic line-search fallback gap. Diagnosis: after testing `alpha_max, alpha_max/2, ..., alpha_max/16`, the fallback returned `alpha_max/32`, a Newton step never evaluated against the residual. Fix: store and return the final evaluated alpha. Verification: monolithic tests pass 26/26; kwavers check and clippy pass; the fallback test forces all candidates to fail and verifies both returned alpha and retained trial state.

- **CLOSED** [patch] Monolithic residual vertical-tree gap. Diagnosis: residual assembly, finite-difference JVP, adaptive line search, and residual tests shared one file despite separate mathematical responsibilities. Fix: replace the flat file with `residual/{mod,compute,jvp,line_search,tests}.rs`; keep the same inherent `MonolithicCoupler` method contracts and add no wrappers or dynamic dispatch. Verification: monolithic tests pass 26/26; kwavers check and clippy pass; every monolithic residual child file is below the 500-line structural limit.

- **CLOSED** [patch] Monolithic utility vertical-tree and squared-norm gap. Diagnosis: stacked-state layout, zero-copy block views, norm reductions, finite-difference Laplacian kernels, and tests shared one utility file; line-search candidate checks also recomputed square roots despite only needing norm ordering. Fix: split utilities into `utils/{mod,layout,block,norm,laplacian,tests}.rs` and use `norm_squared` for the sufficient-decrease test. Verification: monolithic tests pass 27/27; kwavers check and clippy pass; squared-norm test pins exact computed values.

- **CLOSED** [patch] Monolithic coupler vertical-tree gap. Diagnosis: the coupler state definition, constructors, validation contracts, solve loop, plugin mutation, accessors, and tests shared one file plus a test child, obscuring the physics solve lifecycle. Fix: replace the flat file with `coupler/{mod,construction,validation,solve,plugins,accessors,tests}.rs` while keeping one canonical `MonolithicCoupler` type and no wrapper API. Verification: monolithic tests pass 27/27; kwavers check and clippy pass; all coupler child files are below the 500-line structural limit.

- **CLOSED** [patch] Monolithic semantic-tree naming gap. Diagnosis: `monolithic/utils` described a refactoring bucket rather than the represented simulation concepts. Fix: remove `utils` and move responsibilities into `state_vector` for stacked field state, `residual_metric` for Newton norms, and `spatial_operator` for finite-difference Laplacian kernels. Verification: monolithic tests pass 27/27; kwavers check and clippy pass; scan finds no remaining `monolithic::utils` references.

- **CLOSED** [patch] Monolithic configuration-domain gap. Diagnosis: convergence reports, Newton controls, physical coefficients, derived transport laws, and tests shared one config file; invalid coefficients could reach residual assembly and produce divisions by zero or infinite diffusion/heating coefficients. Fix: split `config/{mod,convergence,newton,physics,tests}.rs`, add owning validation methods, and call them from solve preflight. Verification: monolithic tests pass 31/31; kwavers check and clippy pass; value tests assert exact Newton alpha, density, optical-transport, and solve-level physics rejection errors.

- **CLOSED** [patch] Simulation multiphysics residual allocation and norm-consistency gap. Diagnosis: explicit and implicit coupled solves allocated `new - old` arrays and then averaged the absolute update, while monolithic coupling used an L-infinity metric; `FieldCoupler` repeated the allocating mean residual. Fix: add `simulation::multi_physics::residual::max_abs_difference`, route all coupled residual checks through the same streaming O(1)-memory L-infinity metric, and reject shape mismatch or non-finite updates. Verification: multiphysics tests pass 11/11 with exact residual/history assertions; kwavers check passes; clippy `-D warnings` passes after correcting the brain-helmet iterator lints that blocked the package diagnostic gate.

- **CLOSED** [minor] Chapter 27 seismic/FWI simulated-ultrasound nonlinear hemispherical reconstruction and ROI gap. Diagnosis: Chapter 27 exposed simulated data and FWI but used stale 2-D/slice-wise figure artifacts, a slice-plane ring surrogate, lacked nonlinear contrast encoding, and lacked a focused deep-midline reconstruction view for the pons-through-thalamus region. Fix: set the executable chapter path to a bounded `56^3` volume default, deterministic 1024-element hemispherical cap geometry, 3-D source/receiver path lengths, CT-derived axial offsets, five frequencies over 200-800 kHz, eight receiver offsets, weak-Westervelt second-harmonic encoded rows, frequency continuation, Sobolev conditioning, CT-derived path attenuation, matrix-free row application, and target-independent regularized FWI display reconstruction; add a centroid ROI helper module and `fig07_centroid_pons_thalamus_roi`; regenerate all metrics and figures. Evidence: generated metrics report 81,920 measurements, five continuation stages, attenuation enabled, nonlinear harmonic enabled, hemispherical geometry metadata, twelve visible reconstruction stack slices, centroid ROI records for slices 2-24, and visible reconstruction status `true`.

- **CLOSED** [patch] Chapter 27 lacked a therapy-facing taxonomy for histotripsy lesion tracking with seismic imaging. Fix: added subchapters separating active inter-burst RTM/FWI from passive intra-burst cavitation imaging, defined linear time-lapse, multiparameter attenuation, nonlinear tissue-harmonic, subharmonic passive source, bubble-dynamics, elastic/shear, and structural-prior FWI contracts, and recorded which parts are implemented versus pending. Evidence: `docs/book/seismic_fwi_brain_imaging.md` now states RTM/PAM as the real-time localization layer and active time-lapse FWI as the quantitative post-packet lesion-state update.

- **CLOSED** [minor] Chapter 27 lacked executable custom histotripsy RTM/FWI images. Fix: added `pykwavers/examples/book/ch27_histotripsy_fwi_rtm.py`, which obtains the RIRE CT baseline through the pykwavers/RITK wrapper, uses the documented 1024-element receive aperture, simulates compact intrinsic-threshold, shock-elongated, and multi-packet lesion states with deterministic receiver noise, and reconstructs them with 110/160/220 kHz frequency continuation, Huber-robust normal FWI, multiparameter speed/attenuation FWI, weak nonlinear harmonic FWI, passive 110/220/440 kHz RTM, 110 kHz subharmonic cavitation-source FWI, and frequency-gated fusion. Evidence: generated `fig08_histotripsy_custom_reconstruction_scenarios`, `fig09_histotripsy_reconstruction_metrics`, `fig10_histotripsy_passive_band_rtm`, and `histotripsy_monitoring_metrics.json`; fusion Dice/AUPRC/CNR are compact `0.897/0.724/3.74`, elongated `0.826/0.802/3.90`, and multi-packet `0.933/0.995/3.96`.

- **CLOSED** [minor] Kidney/liver histotripsy chapters lacked a CT-derived FWI analysis path for targeting and lesion-state monitoring. Fix: added Chapter 28 with `pykwavers/examples/book/abdominal_fwi/{model,operators,preprocessing,regularization,simulation,transducer}.py` and `ch28_abdominal_histotripsy_fwi.py`; the model extracts the largest tumor-centered KiTS19/LiTS CT plane, builds a CT-textured anatomical support, assembles fundamental path-sum Born receiver rows, half-frequency subharmonic receiver rows, and second-harmonic nonlinear receiver rows with CT-derived attenuation and a HistoSonics-like 256-element therapy aperture plus central imaging receivers, runs a bounded 2-D Westervelt FDTD source-map solve with target-centroid finite-burst timing, drives Rayleigh-Plesset bubble dynamics from the simulated lesion pressure, and uses the same diagonal-preconditioned H1-regularized CG inverse with a 750 kHz upper continuation frequency for baseline targeting, time-lapse lesion perturbations, subharmonic source density, and nonlinear susceptibility. Evidence: generated `docs/book/figures/ch28/fig01_kidney_abdominal_fwi.*`, `fig01_liver_abdominal_fwi.*`, `fig02_kidney_subharmonic_nonlinear_fwi.*`, `fig02_liver_subharmonic_nonlinear_fwi.*`, and `metrics.json`; kidney/liver FOV are `0.202 m`/`0.156 m`, anatomy Pearson are `0.905045240364913`/`0.7933320683973594`, lesion Dice are `1.0`/`0.9545454545454546`, subharmonic Dice are `0.34615384615384615`/`0.7272727272727273`, nonlinear Dice are `0.9615384615384616`/`0.9545454545454546`, Westervelt steps are `940`/`974`, lesion fundamental peaks are `231730.3125 Pa`/`377326.375 Pa`, and the metrics explicitly mark the outputs as synthetic model-consistent simulations rather than measured hardware data or full 3-D adjoint nonlinear FWI.

- **CLOSED** [minor] Same-device ultrasound therapy/imaging lacked a single kwavers-owned CT/NIfTI simulation path spanning HistoSonics-like abdomen, INSIGHTEC-like brain, active finite-frequency inversion, passive receive-only inversion, source-encoded linear acoustic RTM, and pressure-calibrated exposure. Fix: added Chapter 29 with `kwavers::clinical::therapy::theranostic_guidance`, solver-owned reduced kernels in `kwavers::solver::inverse::same_aperture`, CT-derived full-patient placement contexts, one connected abdominal treatment component per single-focus sonication, the RITK-backed `pykwavers.run_theranostic_inverse_from_ritk` wrapper, and `pykwavers/examples/book/ch29_theranostic_fwi_platforms.py`; the clinical Rust module owns CT preprocessing, device placement, exposure synthesis, fusion, placement metrics, and the time-domain RTM trace solve, while `solver::inverse::same_aperture` owns active pitch-catch Born rows, passive subharmonic rows, harmonic/ultraharmonic rows, and graph-H1 PCG. Evidence: generated `docs/book/figures/ch29/fig01_device_placement_on_ct.*`, `fig02_exposure_and_reconstruction.*`, `fig03_brain_helmet_3d_placement.*`, `fig04_reconstruction_dynamic_range_diagnostics.*`, and `metrics.json`; `fig02` starts each brain/kidney/liver row with the CT placement slice plus target/body overlay and transducer coordinates before exposure and reconstruction channels, brain uses 1024 helmet elements at `1.5e5 Pa`, the 3-D helmet samples `72/72` beam paths through CT skull support and stays on the calvarium instead of the neck, kidney/liver use 256 therapy elements plus 64 imaging receivers at `28.0e6 Pa`, the RTM helper injects the configured pressure scale in pascals, records same-aperture baseline/lesion receiver traces through a CT-domain travel-time horizon, and backpropagates trace residuals. `fig04` plus `reconstruction_diagnostics` report common-scale dB sidelobe evidence so finite-frequency aperture rings are not interpreted as extra treated targets. The chapter states the outputs are synthetic model-consistent simulations rather than measured HistoSonics, Verasonics, or INSIGHTEC hardware data.

- **CLOSED** [patch] Chapter 29 theranostic PCG allocated image-sized Laplacian state during every normal-operator and objective evaluation. Fix: `solver::inverse::same_aperture::ActiveGrid` now precomputes active-mask neighbor indices once, `graph_laplacian_into` applies the H1 penalty directly over active voxels, and `solve_tikhonov_h1` reuses row, prediction, normal-operator, and Laplacian buffers. Evidence: Rustdoc records the graph-energy and SPD proofs, same-aperture unit tests assert exact matrix products and graph energy, the PyO3 operator label remains `finite_frequency_same_aperture_graph_laplacian_pcg`, and focused Rust/PyO3 smoke verification passes.

- **CLOSED** [minor] Chapter 29 same-aperture inverse algebra was coupled to clinical CT/anatomy workflow code. Fix: extracted active-support graph indexing, row-major sensitivity algebra, active pitch-catch rows, passive receive-only rows, harmonic/ultraharmonic rows, deterministic noisy simulated data, and graph-H1 PCG into `kwavers::solver::inverse::same_aperture`; `clinical::therapy::theranostic_guidance` now depends on that solver module and keeps only patient/device/exposure/reporting responsibilities. Evidence: `cargo test -p kwavers same_aperture --lib`, `cargo test -p kwavers theranostic_guidance --lib`, `cargo build -p pykwavers --release`, RITK-backed kidney smoke through `pykwavers.run_theranostic_inverse_from_ritk`, and `pytest pykwavers/tests/test_bindings_surface.py::test_public_symbols_are_exposed -q` pass.

- **CLOSED** [patch] Chapter 29 same-aperture inverse still used dense row materialization in the clinical workflow, which did not scale with a 1024-element therapy/receive aperture. Fix: added a solver-owned `LinearOperator` contract, implemented matrix-free `FiniteFrequencyOperator` channels for active pitch-catch, passive subharmonic, harmonic, and ultraharmonic rows, generalized graph-H1 PCG over the operator trait, retained dense `RowMatrix` only for materialized verification, and exposed backend/storage diagnostics through `TheranosticInverseResult` and PyO3. Evidence: focused tests prove matrix-free `matvec`, `t_matvec`, and diagonal values match materialized rows; theranostic tests assert matrix-free storage is below dense-equivalent storage for abdomen and brain; `cargo check`, clippy, and Python compile verification pass.

- **CLOSED** [patch] Chapter 29 reduced inverse did not compress same-aperture receiver rows even though current UCT/FWI source-encoding literature targets wavefield-count and row-count scaling. Fix: added `solver::inverse::same_aperture::EncodedOperator` with exact deterministic normalized row compression `B = C A`, routed active/passive/harmonic/ultraharmonic clinical PCG channels through encoded matrix-free operators, exposed `inverse_encoding_rows_per_code`, `encoded_measurements`, and `unencoded_measurements` through PyO3, and documented the encoded normal-equation theorem without changing reduced-branch FWI flags. Evidence: same-aperture tests compare encoded forward, adjoint, diagonal, and measurement products against a materialized oracle; theranostic tests assert encoded row reduction and metric propagation.

- **CLOSED** [patch] Chapter 29 public API and figure metadata over-claimed full-waveform inversion. Fix: renamed the PyO3 entry point to `run_theranostic_inverse_from_ritk` without retaining a compatibility alias, renamed Rust public types to `TheranosticInverseConfig`/`TheranosticInverseResult`, added `inverse_model_family`, `is_full_wave_inversion`, and `uses_nonlinear_wave_propagation` metadata, changed figure labels to finite-frequency inverse and linear acoustic RTM, flattened the RTM forward-history store into one contiguous buffer, made the RTM source/horizon physically tied to configured pressure and CT travel time instead of display scaling, and added value-tested L2/Charbonnier residual selection for the RTM adjoint source. Evidence: binding-surface tests reject the old wrapper name, theranostic tests assert the non-FWI/non-nonlinear flags, and Chapter 29 plotting tests reject `FWI` in the reduced inverse figure labels.

- **CLOSED** [patch] Chapter 29 linear acoustic RTM residual used unbounded least-squares receiver injection despite noisy same-device monitoring requirements. Fix: added `clinical::therapy::theranostic_guidance::misfit`, implemented `WaveformMisfit::{L2, Charbonnier}`, used the Charbonnier derivative as the default adjoint source with scale derived from observed-trace RMS and configured noise fraction, exposed misfit selection through PyO3, and recorded waveform misfit, scale, and objective in Chapter 29 metrics. Evidence: value tests verify exact L2 residual/objective and Charbonnier bounded influence; theranostic tests assert the default robust misfit metadata.
- **CLOSED** [minor] Chapter 29 nonlinear Figure 5 was too coarse and the nonlinear branch remained single-parameter. Fix: extended `clinical::therapy::theranostic_guidance::nonlinear3d` with deterministic source-encoded focused Westervelt shots, discrete-adjoint gradients for `c` and `beta`, CT/segmentation-derived target-ROI inversion masks, body-restricted `H1` regularization, Sobolev-smoothed gradients, Rust-side multiparameter FWI/cavitation fusion scoring, PyO3 controls for source encoding and regularization, and a Figure 5 layout matching the Figure 2 CT/exposure/target/reconstruction/fusion grammar. The generator now runs Figure 5 at the same per-case simulation grids as Figure 2 by default: `48^3` for brain and `52^3` for kidney/liver, while preserving explicit nonlinear-grid environment overrides. The nonlinear adjoint uses four rolling adjoint volumes instead of dense `(steps + 1)` adjoint-state storage, and retained forward states now use exact sparse checkpoints plus bounded interval replay instead of one stored pressure volume per timestep. Evidence: focused nonlinear Rust test passes with finite objective monotonicity and positive Rayleigh-Plesset source; checkpoint tests prove bitwise replay equivalence and checkpoint-interval-invariant `c/beta` gradients; PyO3 release build passes; regenerated metrics record source encoding, regularization controls, checkpoint interval, and the `48/52/52` nonlinear grid sizes. Residual: default full-run runtime is about 42 min locally after checkpoint replay, so replay fusion and line-search caching remain open performance work.

- **CLOSED** [patch] Chapter 29 nonlinear volume preparation exceeded the 500-line structural limit. Fix: split attenuation law derivation and centroid utilities into `volume/attenuation.rs` and `volume/centroid.rs`; all nonlinear 3-D leaf files are now below 500 lines. Evidence: focused nonlinear tests and clippy pass after the split.
- **CLOSED** [patch] Chapter 29 nonlinear 3-D structural-limit regression (2026-05-14). Diagnosis: after the 2026-05-14 attenuation/centroid split, `clinical::therapy::theranostic_guidance::nonlinear3d::volume.rs` re-grew to 521 lines and the parallel `absorption.rs` reached 555 lines through accumulated Treeby-Cox 2010 absorption infrastructure. Fix: split `volume` into an 86-line facade plus SRP children `volume/{validation,bbox,mask,resample,material}` covering input validation, bounding-box cropping, body/target/inversion mask construction, lattice resampling/trilinear, and CT-derived sound-speed/density/beta/attenuation maps respectively; split `absorption` into a 115-line facade carrying module docs and `FractionalLaplacianAbsorption` + `AbsorptionBuilder` struct definitions, plus `absorption/{construction,spectrum,apply,tests}` children covering `maybe_new` + `new` + `representative_y`, the `build_k_power_spectrum` + `spectral_filter` free helpers, the `apply` / `apply_transpose` / `reset` / `y_exponent` method block, and the Treeby-Cox coefficient + transpose-identity tests. External callers `nonlinear3d::adjoint` and `nonlinear3d::forward` keep the same `use super::absorption::{AbsorptionBuilder, FractionalLaplacianAbsorption}` import path. Evidence: every nonlinear3d non-test source file is now `<= 500` lines (largest: `forward.rs` at 500, `cavitation.rs` at 451; volume children `<= 152`; absorption children `<= 149`); `cargo check -p kwavers --lib` clean; `cargo clippy -p kwavers --lib --no-deps -- -D warnings` clean (one `iter_cloned_collect` flagged and fixed); 20/20 default nonlinear3d tests pass with 3 Tier-2 ignored; `nonlinear_3d_westervelt_fwi_and_cavitation_inverse_are_input_sensitive` passes 1/1.

- **CLOSED** [minor] The ultrasound physics book lacked an intravascular ultrasound chapter with an explicit dataset, catheter design, and coupled imaging/therapy simulation. Fix: added Chapter 30 with `docs/book/intravascular_ultrasound.md`, `pykwavers/examples/book/ch30_intravascular_ultrasound.py`, manifest/README registration, and tests. The chapter records the public IVUS-Net/IVUS Challenge segmentation corpus as the external contour-validation target, computes a deterministic coronary-wall phantom, implements a 64-element 20 MHz imaging ring plus 1.5 MHz side-looking microbubble therapy sector, generates radial B-mode and scan-converted images, and computes pressure, deposition, and thermal fields from the same phantom. Evidence: generated `docs/book/figures/ch30/fig01_dataset_and_anatomy.*`, `fig02_transducer_design.*`, `fig03_ivus_bmode_simulation.*`, `fig04_microbubble_therapy_map.*`, `fig05_intravascular_usage_sequence.*`, and `metrics.json`; direct value checks pass with wall mean B-mode `0.7991358070999905`, lumen mean `0.7361069713462705`, therapy MI `0.23899887525938462`, peak thermal rise `0.31508926186346214 C`, and deposition ratio `1786.1169113191795`. Full pytest collection is blocked by a pre-existing `pykwavers._pykwavers` extension symbol mismatch for `plan_brain_helmet_placement_from_ritk_ct`.

- **CLOSED** [minor] Fluid-structure coupling inactive-tree and ghost-allocation gap. Diagnosis: the FSI implementation existed on disk but was not declared by `solver::multiphysics`, so its tests and diagnostics did not run; inside the solver, every coupling iteration cloned pressure and traction ghost fields and every exchange allocated replacement ghost arrays. Fix: activate the module under `solver::multiphysics::fluid_structure`, replace the multi-argument interface constructor with `FsiInterfaceSpec`, add solver-owned previous ghost workspaces, and update ghost exchange to fill existing pressure/traction buffers in place. Verification: FSI tests pass 9/9; kwavers check and clippy pass; line counts are 36/76/192/227/326/194 for touched FSI files.

## Remaining Open Gaps (post 2026-05-10)

- **CLOSED** [minor] Split-field elastic PML for `ElasticPstdOrchestrator`: the active `ElasticSplitFieldPml` implements the Berenger/Collino-Tsogka directional split-field integrator, `SplitFieldState` stores the required velocity and stress subfields, `ElasticPstdOrchestrator::set_split_field_pml` routes propagation through `propagate_split_field_step`, and value tests cover coefficient validity, quiescent-state invariance, zero-thickness equivalence, and outgoing-wave attenuation. The 2026-05-11 hardening increment consolidates construction through `ElasticPmlSpec` and removes the split-PML unwrap path.
- **CLOSED** [minor] k-space correction (Tabei et al. 2002) for elastic PSTD: `kappa[i,j,k] = sinc(c_ref·dt·|k|/2)` pre-computed from the medium's max P-wave speed, applied to every spectral derivative in both `apply_stress_update_in_place` and `apply_velocity_update_in_place`; `kappa` field added to `StressUpdateParams` and `VelocityUpdateParams`; helper functions `max_p_wave_speed` and `build_kappa` in new `elastic_orchestrator/kspace.rs`; root-cause fix for `wavenumber_axis(n=1)` returning `−dk` instead of 0 (singleton axes in 1D/2D grids had a spurious non-zero wavenumber that inflated kappa args by √9×); 4 new analytical value-semantic tests: DC-mode kappa = 1, (0,1] containment, Nyquist sinc(CFL·π/2) exact match, and amplitude-preservation under propagation; 2962/2962 lib tests PASS.
- **CLOSED** [minor] k-wave-python parity completion (2 missing → 0): `ivp_saving_movie_files` (128×128 heterogeneous medium, C_FAST=1800 m/s, RHO_HIGH=1200 kg/m³ at column 32=Ny//4, two disc sources, Cartesian circular sensor r=4mm 50 points, records p and p_final; density boundary at index 32 differs by 1 from ivp_heterogeneous_medium) and `na_optimising_performance` (256×256 homogeneous medium, image-derived p0 from EXAMPLE_source_two.bmp scaled to 2 Pa, Cartesian circular sensor r=4.5mm 100 points near PML, records p and p_final). Both compare scripts created (`ivp_saving_movie_files_compare.py`, `na_optimising_performance_compare.py`) with NPZ caching, C→Fortran sensor-row permutation, p_final field 2D comparison, and 2×3 panel plots. Fast analytical pytest suites (16 tests each, 32 total) PASS covering grid/medium constants, source construction, and Cartesian sensor mask geometry. Full simulation parity gated on KWAVERS_RUN_SLOW=1. Upstream Python example set now 100% covered.
- **[minor]** MATLAB-only physics with k-wave-python gap: `pr_2D_adjoint`, `ivp_setting_initial_gradient`, `us_transducer_as_sensor`, `sd_sensor_directivity_2D`, `ivp_sensor_frequency_response`, plus 7 photoacoustic time-reversal variants (autofocus, absorption_compensation, iterative, circular_sensor, bandlimited_sensors, time_variant_filtering, 3D spherical_sensor).

## Session 2026-05-12 Transcranial Brain FUS Planning

- **CLOSED** [minor] Pykwavers lacked a transcranial brain focused-ultrasound planning example analogous to the histotripsy and BBB chapters. Fix: chapter 25 now loads local cranial CT/T1/MNI volumes, writes source manifests for NiiVue/MNI/TCIA, attempts RITK registration, builds a 1024-element 650 kHz phased array, applies skull path phase correction, computes pressure/MI/cavitation probability, integrates Pennes CEM43 thermal dose at an MNI VIM-like coordinate, and supports segmented CFB-GBM subspot planning when local data are present. Verification: 4 value-semantic Python tests pass, compileall passes, line-count audit passes, and chapter execution emits `metrics.json` plus figures under `docs/book/figures/ch25`.
- **CLOSED** [minor] RITK execution was blocked by the local Python binding state. Root cause: implemented segmentation/distance-transform APIs were not re-exported from `ritk-core`, `compute_from_values` was private while used by `ritk-python`, and Windows extension loading lacked deterministic DLL-directory registration. Fix: exported the authoritative core APIs, made statistics computation callable by the binding, added `os.add_dll_directory` discovery in `ritk.__init__`, built/installed the local wheel, and changed chapter 25 to read CT/MRI/MNI NIfTI sources through `ritk.io.read_image` before `multires_syn_register`. Verification: RITK smoke tests pass 16/16, `cargo check -p ritk-python` passes, and chapter metrics record `registration.executed=true`.
- **RESIDUAL** [patch] RITK Windows GNU now carries static runtime preference flags in `.cargo/config.toml`, but the verified `ritk-python` extension still depends on `libstdc++-6.dll`. Evidence: `dumpbin /dependents` reports `libstdc++-6.dll`; forcing `cargo:rustc-link-lib=static=stdc++` in `ritk-io/build.rs` breaks final linking with unresolved CharLS C++ ABI symbols. Current accepted state preserves a buildable wheel while using `+crt-static`, `-static-libstdc++`, and `-static-libgcc` where the linker accepts them.
- **CLOSED** [minor] Clinical HIFU treatment planning used a single-focus adequacy heuristic and did not produce a volumetric sonication/subspot schedule. Fix: `HIFUPlanner::plan_sonication_schedule` now emits `SonicationSchedule` with target-plus-margin subspots, pitch derived from the focal FWHM ellipsoid corner-bound, per-subspot dwell time, and minimum subspot CEM43/temperature; `plan_treatment` feasibility now checks schedule coverage and per-subspot dose without changing the existing plan struct layout. Verification: HIFU planning tests pass 12/12, kwavers lib check passes, clippy `-D warnings` passes, and touched HIFU files remain below 500 lines.
- **CLOSED** [minor] HIFU and BBB chapters were not synchronized as peer therapy chapters to Chapter 21 histotripsy. Fix: Chapter 24 is verified through the manifest runner as the BBB-opening biophysics chapter; Chapter 25 is titled, documented, and tested as transcranial HIFU plus BBB treatment planning; the GBM branch now computes BBB acoustic dose, permeability, stable-cavitation probability, inertial-cavitation risk, and opened tumor mask from segmented subspots. Verification: focused Python tests pass 8/8, Chapters 24/25 compile, Chapter 24 emits six figure pairs, and Chapter 25 emits HIFU planning figures plus metrics with `registration.executed=true`.
- **CLOSED** [minor] GBM branch execution was blocked by data availability. Fix: downloaded UPenn-GBM `sub-002` from the NIH/TCIA GitHub release into `data/upenn_gbm_sample`, added source/license provenance, and extended discovery so MRI-backed UPenn-GBM executes BBB subspot treatment when CT-backed CFB-GBM is absent. Evidence: Chapter 25 metrics record `gbm.executed=true`, `dataset="UPenn-GBM"`, 3092 subspots, 100% subspot coverage, and 86.934% BBB-opened tumor fraction.
- **CLOSED** [minor] Transcranial skull acoustics used a binary skull-mask delay model, which did not account for CT-derived attenuation or impedance transmission. Fix: downloaded and converted RIRE patient 109 CT, changed the default CT source to the RIRE skull map when present, added CT HU-to-density/sound-speed/attenuation mapping, accumulated ray travel time through CT, applied normal-incidence impedance transmission at material changes, and weighted Rayleigh elements by CT-derived amplitude. Evidence: Chapter 25 metrics record `mean_amplitude_weight=0.40291847271890213` and `min_amplitude_weight=0.014716668699769548`, and focused tests assert amplitude variation from CT skull paths.
- **CLOSED** [minor] HIFU and BBB planning had an over-constrained data contract that treated MRI as required for BBB execution. Fix: HIFU now explicitly uses CT plus registered atlas; BBB planning now treats CT plus CT-space segmentation as sufficient, with a RIRE CT-space segmentation fixture exercising the path. Evidence: focused tests pass 9/9 and a direct Chapter 25 execution records `gbm.ct_backed=true`, `segmentation_space="ct"`, 2182 subspots, and full CT-space target coverage.
- **CLOSED** [minor] MRI-space GBM fallback lacked explicit sample-CT registration and visual QC. Fix: added NIfTI-affine CT-to-MRI resampling, corrected resampled affine spacing metadata, and made Chapter 25 emit `fig06_affine_ct_to_mri_qc` plus NMI/edge-overlap metrics whenever sample CT is reused for an MRI-space segmentation. Evidence: focused tests assert affine metadata preservation and CT peak transfer to the MRI lattice.
- **CLOSED** [patch] CT/T1/MNI registration validation displayed modality-native voxel planes instead of a shared physical plane. Root cause: `_ritk_normalized_nifti_image` read NIfTI files and zoomed arrays by shape, ignoring coordinate affines. Fix: T1 and MNI are now affine-initialized onto the CT lattice, RITK refines those CT-space moving images, outputs carry the CT affine/spacing, and `fig01_registered_ct_mri_mni` overlays skull/brain contours on the same axial target plane. Evidence: regenerated figure records `foreground_extent_affine_resampling + ritk.registration.multires_syn_register`.
- **CLOSED** [patch] Registration initialization did not test AP/LR/SI reflection candidates and used NCC alone for multimodal QC. Fix: foreground-extent affine initialization now searches all axis-reflection candidates by fixed/moving foreground Dice before RITK refinement, and metrics include NMI plus normalized MSE for T1 and atlas before/after registration. Evidence: an AP-reflection regression test maps an inverted moving mask exactly onto the fixed mask; regenerated metrics show NMI/MSE values alongside NCC.
- **CLOSED** [minor] CT/MR/MNI transform graph still allowed cross-subject image pairs and independent atlas-to-CT fitting. Root cause: the previous default used an RIRE CT, unrelated T1 MRI, and MNI atlas, so rigid/affine/reflection searches could improve scalar scores while violating anatomical correspondence. Fix: converted RIRE patient 109 MR-T1/MR-T2 to NIfTI, corrected MetaImage raw ordering to `(z, y, x) -> (x, y, z)`, made the same-patient MR-T1 the default subject MRI for the RIRE CT skull map, and initialized MNI against subject MRI already represented on the CT lattice. Evidence: Chapter 25 metrics record `same_patient_nifti_affine_world_resampling + subject_mri_atlas_affine + ritk.registration.multires_syn_register`.
- **CLOSED** [patch] Registration masks and deformable acceptance were still weak after same-patient data replacement. Root cause: `ct > -300` misclassified zero-HU RIRE background as brain, MNI was initialized against broad T1 foreground instead of intracranial anatomy, the same-patient atlas affine could accept mirrored fits, the adapter passed nibabel-style XYZ arrays into RITK's ZYX image memory contract, and RITK SyN outputs were accepted even when the primary NMI metric regressed. Fix: derive CT brain masks from filled skull boundaries, fit MNI to the CT-derived intracranial mask intersected with T1 foreground, forbid LR/AP/SI reflections for the same-patient atlas path, transpose XYZ-to-ZYX at `ritk.Image` construction and ZYX-to-XYZ after `to_numpy()`, add NMI translation refinement after foreground affine initialization, and guard RITK outputs with NMI plus weighted multimodal score monotonicity. Evidence: `ritk.io.read_image` matches nibabel MNI data after the explicit XYZ/ZYX transpose, regenerated Chapter 25 metrics record `metric_guarded_ritk_syn(t1=rejected,atlas=rejected)` with non-regressed NMI, and the QC figure now shows axial/coronal/sagittal target planes.
- **CLOSED** [minor] GBM treatment ingest lacked a reproducible modality-bridge contract. Root cause: CT-space segmentation, MRI-space UPenn-GBM, sample-CT QC, and future synthetic modality outputs were implicit branches. Fix: added `transcranial_planning.modality_bridge`, `modality_bridge_manifest.json`, explicit CT/segmentation, MRI/segmentation, and CT/MRI pairing requirements, cWDM/SLaM-DiMM/NV-Segment-CTMR reference records, and rejection rules that prevent sample or synthetic CT from being treated as measured same-patient skull acoustics. Evidence: focused tests assert CT-space readiness, UPenn MRI-only non-CT-backed status, and external synthesis boundaries.
- **CLOSED** [patch] GBM ingest still encoded missing MRI channels through fake CT-backed T1-Gd/FLAIR paths and over-required both T1-Gd and FLAIR for MRI-space execution. Root cause: `GbmCasePaths` made T1-Gd and FLAIR non-optional, forcing CT-only fixtures to supply surrogate paths and forcing MRI cases to satisfy a complete two-channel subset. Fix: introduced `GbmLoadedCase`, made MRI channel paths optional, selected one real planning reference from the available modality set, accepted CT plus CT-space segmentation without MRI, accepted MRI-space segmentation with any real MRI volume, and added Holder-MI incomplete-MRI plus TextBraTS references to the bridge. Evidence: `pykwavers\.venv\Scripts\python.exe -m pytest pykwavers\tests\test_transcranial_planning.py -q` passes 24/24; Chapter 25 execution records `available_modalities=["ct","segmentation"]`, `t1gd=null`, `flair=null`, `simulation_ready=true`, and `subspots=2182`.
- **OPEN** [minor] Same-patient CT-backed GBM execution remains absent. Evidence: RIRE patient 109 provides same-patient CT/MR but no tumor segmentation; UPenn-GBM provides real MRI and segmentation but no CT; the CT-space segmentation fixture verifies BBB mechanics but is not a clinical GBM case. Required closure: add a licensed minimal CFB-GBM extraction or TCIA/NBIA fetch step for same-patient CT/MRI/segmentation planning.
- **CLOSED** [patch] Book chapters ch3, ch5, ch6, ch7, ch27, ch29, ch31 contained mathematical errors, incomplete proofs, and algorithm descriptions inconsistent with the Rust implementation. Closed: (1) Ch31 metrics channel_keys matched with Rust dict keys; bowl radius theorem corrected to R=d_f/cos(θ_max); exterior flood-fill BFS + approach-angle penalty algorithm documented. (2) Ch29 same-device aperture contract and operator rank theorem added; period-doubling observable derived from Floquet theory; minimal PyO3 usage example added. (3) Ch5 Cramér-Rao Jacobian derivation completed; kr≫1 far-field condition added with numerical bound; shell stiffness scope note added. (4) Ch7 CEM43 monotonicity proof made self-contained (R^x>0); KalmanFilter 4-state vector specified; drug uptake dilute-bubble scope added. (5) Ch27 Born linearity Theorem 27.1 added with proof; elastic FWI "Pending" replaced with explicit deferral note; minimal usage example added. (6) Ch6 ΔT unit fixed (°C not °C/s); focal gain simplified to G=ka²/(2R_f); 479→60-80°C reduction correctly attributed to thermal conduction + perfusion; CEM43 time units corrected (2184 min not 131,072 min). (7) Ch3 Burgers Theorem 3.6 proof expanded with explicit τ integration; Kepler-Jacobi-Anger identity proof sketch added; second-harmonic P₂∝z derivation completed; Kuznetsov Eq. 3A.1 non-standard "·2" notation fixed.
