--- a/src/physics/chemistry/ros_plasma/plasma_reactions.rs
+++ b/src/physics/chemistry/ros_plasma/plasma_reactions.rs
@@ -59,7 +59,7 @@
         chemistry.add_standard_reactions();
-        chemistry.initialize_concentrations();
+        let _ = chemistry.initialize_concentrations();
 
@@ -187,6 +187,7 @@
             self.concentrations
                 .insert("e-".to_string(), 1e-15 * total_conc);
         }
+        Ok(())
     }
 
--- a/src/physics/state.rs
+++ b/src/physics/state.rs
@@ -86,8 +86,8 @@
     fn deref(&self) -> &Self::Target {
-        // Cannot implement - use view() method
+        unimplemented!("Use view() method instead - Deref cannot return temporary")
     }
 }
 
@@ -95,7 +95,7 @@
     fn deref_mut(&mut self) -> &mut Self::Target {
-        // Cannot implement - use view_mut() method
+        unimplemented!("Use view_mut() method instead - DerefMut cannot return temporary")
     }
 }

--- a/src/solver/plugin_based/solver.rs
+++ b/src/solver/plugin_based/solver.rs
@@ -166,7 +166,7 @@
             let result = plugin_manager.execute(
                 fields_array,
                 &self.grid,
-                &**self.medium,  // Deref Arc<dyn Medium> to &dyn Medium
+                self.medium.as_ref(),  // Get &dyn Medium from Arc
                 self.time.dt,
                 t
             );